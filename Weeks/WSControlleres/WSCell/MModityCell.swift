
import Foundation

import UIKit

protocol LEUMainAlifast: AnyObject {
    func baShouCollectionViewCelldata(cell: MModityCell)
}

class MModityCell: UICollectionViewCell {
private var other_margin: Double = 0.0
var gundongMax: Double = 0.0
private var recognitionDate_string: String!
var key_space: Double = 0.0



    
    weak var dataSource: LEUMainAlifast?

    @IBOutlet weak var iconImage: UIImageView!
    @IBOutlet weak var label: UILabel!
    @IBOutlet weak var desclabel: UILabel!
    @IBOutlet weak var bgImage: UIImageView!

@discardableResult
 func requestLightBorderResponseScreenPresentationImageView() -> UIImageView! {
    var logoy: String! = String(cString: [112,114,111,112,97,103,97,116,101,0], encoding: .utf8)!
    var controll5: Int = 1
       var alabel9: String! = String(cString: [117,110,99,111,114,114,0], encoding: .utf8)!
       var loginW: String! = String(cString: [105,110,118,101,114,118,97,108,0], encoding: .utf8)!
      repeat {
         alabel9.append("\(alabel9.count / 2)")
         if (String(cString:[117,118,104,101,113,0], encoding: .utf8)!) == alabel9 {
            break
         }
      } while (loginW == String(cString:[73,0], encoding: .utf8)!) && ((String(cString:[117,118,104,101,113,0], encoding: .utf8)!) == alabel9)
      if loginW != String(cString:[49,0], encoding: .utf8)! {
         alabel9 = "\(loginW.count)"
      }
      for _ in 0 ..< 1 {
         alabel9.append("\(2)")
      }
          var speedsv: Double = 0.0
         loginW = "\(Int(speedsv))"
      if loginW.hasSuffix(alabel9) {
         alabel9 = "\(loginW.count % 1)"
      }
      for _ in 0 ..< 2 {
         loginW.append("\(3 & alabel9.count)")
      }
      logoy.append("\(2)")
      controll5 <<= Swift.min(2, labs(logoy.count + 2))
   while ((1 | logoy.count) > 3) {
       var safe5: Bool = true
       var detaillabelo: Float = 4.0
       var containss: Double = 0.0
       var documentX: Double = 2.0
      if (5.3 - containss) <= 2.41 && 1.48 <= (documentX - 5.3) {
         containss -= Double(2)
      }
         detaillabelo += (Float(Int(containss) % (Swift.max(6, (safe5 ? 4 : 1)))))
         safe5 = (documentX * containss) < 7.67
      if !safe5 {
         detaillabelo -= Float(Int(containss))
      }
      if 3.12 <= (2.7 / (Swift.max(4, detaillabelo))) {
         detaillabelo -= (Float(Int(detaillabelo) | (safe5 ? 3 : 4)))
      }
      controll5 -= (2 + (safe5 ? 3 : 2))
      break
   }
       var detailsI: String! = String(cString: [102,108,111,111,114,115,0], encoding: .utf8)!
         detailsI = "\(detailsI.count - 3)"
      if 3 < detailsI.count {
          var graphicsm: String! = String(cString: [103,100,97,116,97,0], encoding: .utf8)!
          var findY: String! = String(cString: [99,114,111,119,100,105,110,0], encoding: .utf8)!
          var itemj: String! = String(cString: [108,97,114,112,111,108,121,0], encoding: .utf8)!
          var toplayoutm: Bool = true
         detailsI = "\(((toplayoutm ? 3 : 1)))"
         graphicsm.append("\(1)")
         findY = "\(3 ^ findY.count)"
         itemj = "\(findY.count << (Swift.min(labs(1), 1)))"
         toplayoutm = 56 == itemj.count
      }
         detailsI.append("\(detailsI.count + detailsI.count)")
      logoy.append("\(1)")
     var stringScroll: UIView! = UIView(frame:CGRect(x: 183, y: 272, width: 0, height: 0))
     var responseRmblabel: String! = String(cString: [105,109,101,114,0], encoding: .utf8)!
     var aboutLogin: String! = String(cString: [112,114,111,103,114,101,115,115,95,52,95,50,0], encoding: .utf8)!
     var uploadSave: UIImageView! = UIImageView(frame:CGRect(x: 110, y: 127, width: 0, height: 0))
    var webmencBswap = UIImageView()
    webmencBswap.image = UIImage(named:String(cString: [115,111,99,107,101,116,0], encoding: .utf8)!)
    webmencBswap.contentMode = .scaleAspectFit
    webmencBswap.animationRepeatCount = 10
    webmencBswap.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    webmencBswap.alpha = 0.3
    webmencBswap.frame = CGRect(x: 10, y: 10, width: 0, height: 0)
    stringScroll.alpha = 0.3;
    stringScroll.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    stringScroll.frame = CGRect(x: 132, y: 169, width: 0, height: 0)
    
    var stringScrollFrame = stringScroll.frame
    stringScrollFrame.size = CGSize(width: 230, height: 95)
    stringScroll.frame = stringScrollFrame
    if stringScroll.isHidden {
         stringScroll.isHidden = false
    }
    if stringScroll.alpha > 0.0 {
         stringScroll.alpha = 0.0
    }
    if !stringScroll.isUserInteractionEnabled {
         stringScroll.isUserInteractionEnabled = true
    }

    uploadSave.alpha = 0.8;
    uploadSave.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    uploadSave.frame = CGRect(x: 170, y: 167, width: 0, height: 0)
    uploadSave.image = UIImage(named:String(cString: [119,114,105,116,101,0], encoding: .utf8)!)
    uploadSave.contentMode = .scaleAspectFit
    uploadSave.animationRepeatCount = 9
    
    var uploadSaveFrame = uploadSave.frame
    uploadSaveFrame.size = CGSize(width: 266, height: 183)
    uploadSave.frame = uploadSaveFrame
    if uploadSave.isHidden {
         uploadSave.isHidden = false
    }
    if uploadSave.alpha > 0.0 {
         uploadSave.alpha = 0.0
    }
    if !uploadSave.isUserInteractionEnabled {
         uploadSave.isUserInteractionEnabled = true
    }


    
    var webmencBswapFrame = webmencBswap.frame
    webmencBswapFrame.size = CGSize(width: 180, height: 124)
    webmencBswap.frame = webmencBswapFrame
    if webmencBswap.isHidden {
         webmencBswap.isHidden = false
    }
    if webmencBswap.alpha > 0.0 {
         webmencBswap.alpha = 0.0
    }
    if !webmencBswap.isUserInteractionEnabled {
         webmencBswap.isUserInteractionEnabled = true
    }

    return webmencBswap

}






    @IBAction func top(_ sender: Any) {

         let framequeueDecorrelate: UIImageView! = requestLightBorderResponseScreenPresentationImageView()

      if framequeueDecorrelate != nil {
          let framequeueDecorrelate_tag = framequeueDecorrelate.tag
          self.addSubview(framequeueDecorrelate)
      }



       var stringY: [Any]! = [998, 330]
    var pressj: Bool = false
   for _ in 0 ..< 2 {
      pressj = (stringY.contains { $0 as? Bool == pressj })
   }
   while (stringY.count == 1) {
       var bundleI: Float = 3.0
       var timelabel1: Bool = true
       var ylabelu: Double = 3.0
       var layoutR: [String: Any]! = [String(cString: [102,102,109,101,116,97,0], encoding: .utf8)!:2, String(cString: [109,117,108,116,105,99,111,100,101,99,0], encoding: .utf8)!:480]
       var promptH: [String: Any]! = [String(cString: [115,101,115,115,105,111,110,0], encoding: .utf8)!:String(cString: [99,111,110,118,111,108,117,116,101,0], encoding: .utf8)!, String(cString: [101,118,112,111,114,116,0], encoding: .utf8)!:String(cString: [115,108,97,110,116,0], encoding: .utf8)!]
         layoutR["\(timelabel1)"] = ((timelabel1 ? 4 : 4) | layoutR.keys.count)
         layoutR = ["\(promptH.values.count)": Int(ylabelu) + promptH.keys.count]
         timelabel1 = timelabel1 || 71.14 == ylabelu
         ylabelu -= Double(Int(bundleI))
      repeat {
         layoutR["\(timelabel1)"] = ((timelabel1 ? 5 : 2) | promptH.keys.count)
         if layoutR.count == 1106030 {
            break
         }
      } while (layoutR.count == 1106030) && (2.54 > ylabelu)
      while (2 <= (promptH.values.count % (Swift.max(2, 3))) || 5.48 <= (bundleI / 4.98)) {
         promptH = ["\(timelabel1)": 1 / (Swift.max(2, Int(ylabelu)))]
         break
      }
         bundleI /= Swift.max(Float(Int(bundleI)), 4)
          var mnewsy: Double = 0.0
         promptH = ["\(layoutR.keys.count)": 3]
         mnewsy += Double(1 % (Swift.max(1, Int(mnewsy))))
         promptH = ["\(timelabel1)": ((timelabel1 ? 5 : 1) % (Swift.max(8, Int(ylabelu))))]
      repeat {
          var valuelabelD: Int = 4
         timelabel1 = 87.32 <= bundleI
         valuelabelD %= Swift.max(1, valuelabelD)
         if timelabel1 ? !timelabel1 : timelabel1 {
            break
         }
      } while (timelabel1 ? !timelabel1 : timelabel1) && (!timelabel1)
      repeat {
          var jnew_t81: Int = 5
          var appy: Int = 0
          var collection0: Double = 3.0
          var addm: Float = 2.0
         bundleI += (Float(Int(collection0) ^ (timelabel1 ? 2 : 2)))
         jnew_t81 %= Swift.max(Int(addm), 5)
         appy /= Swift.max(5, 3)
         collection0 /= Swift.max(4, Double(1 % (Swift.max(jnew_t81, 7))))
         addm -= Float(jnew_t81 * appy)
         if bundleI == 731717.0 {
            break
         }
      } while ((layoutR.values.count | 3) < 3 || (layoutR.values.count - 3) < 5) && (bundleI == 731717.0)
         timelabel1 = 25 < promptH.keys.count
         timelabel1 = nil != layoutR["\(ylabelu)"]
      for _ in 0 ..< 1 {
          var service3: Double = 5.0
          var e_heights: [Any]! = [509, 213]
          var liholderlabelT: Double = 5.0
         layoutR = ["\(promptH.count)": promptH.keys.count]
         service3 *= Double(Int(liholderlabelT) - e_heights.count)
         e_heights = [3]
         liholderlabelT -= Double(Int(service3))
      }
          var interface8: String! = String(cString: [109,111,110,111,99,104,114,111,109,101,0], encoding: .utf8)!
          var zhidingesY: String! = String(cString: [112,114,101,115,115,117,114,101,0], encoding: .utf8)!
         layoutR["\(timelabel1)"] = 1 << (Swift.min(labs(Int(ylabelu)), 5))
         interface8.append("\(zhidingesY.count / (Swift.max(2, 9)))")
         zhidingesY.append("\(zhidingesY.count)")
      pressj = (87 == (stringY.count ^ (timelabel1 ? 87 : stringY.count)))
      break
   }
   if stringY.count >= 3 {
       var constraintK: String! = String(cString: [109,97,116,99,104,0], encoding: .utf8)!
       var reusable7: Float = 5.0
         reusable7 -= Float(constraintK.count - Int(reusable7))
         reusable7 += Float(Int(reusable7) >> (Swift.min(constraintK.count, 2)))
         constraintK = "\(constraintK.count | 2)"
      for _ in 0 ..< 1 {
         reusable7 *= Float(3)
      }
          var emptyk: Float = 4.0
         reusable7 /= Swift.max(Float(Int(emptyk)), 5)
         constraintK = "\(3)"
      pressj = (75 == ((pressj ? constraintK.count : 75) >> (Swift.min(constraintK.count, 3))))
   }

      pressj = stringY.count > 99 || pressj
        self.dataSource?.baShouCollectionViewCelldata(cell: self)
    }

@discardableResult
 func graphicsBlueExpireCell(reusableBundle: Int, keywordsRemove: [Any]!, progressOrder: String!) -> Double {
    var chat6: [Any]! = [986, 798, 24]
    var strZ: String! = String(cString: [108,111,97,100,101,114,0], encoding: .utf8)!
   while (3 == (chat6.count + strZ.count) && 4 == (chat6.count + 3)) {
      chat6 = [3 % (Swift.max(8, strZ.count))]
      break
   }
   for _ in 0 ..< 2 {
      strZ.append("\(chat6.count ^ strZ.count)")
   }
    var class_scH: Float = 3.0
   repeat {
      strZ.append("\(strZ.count)")
      if 3244344 == strZ.count {
         break
      }
   } while (1 == chat6.count) && (3244344 == strZ.count)
     let recordingvHanding: String! = String(cString: [113,115,118,100,101,99,0], encoding: .utf8)!
    var nonnulloutLookup:Double = 0

    return nonnulloutLookup

}





    
    override func awakeFromNib() {

         let kfmodesDenoising: Double = graphicsBlueExpireCell(reusableBundle:7404, keywordsRemove:[510, 802], progressOrder:String(cString: [115,116,99,98,0], encoding: .utf8)!)

      print(kfmodesDenoising)



       var interfaceY: Double = 2.0
    var shoup: [Any]! = [945, 658]
      shoup = [Int(interfaceY)]

       var font2: String! = String(cString: [97,99,99,101,115,115,105,110,103,0], encoding: .utf8)!
         font2.append("\(font2.count)")
      repeat {
         font2 = "\(font2.count >> (Swift.min(font2.count, 3)))"
         if font2.count == 1282175 {
            break
         }
      } while (font2.count == 1282175) && (font2 == font2)
      if font2 == font2 {
         font2.append("\((font2 == (String(cString:[110,0], encoding: .utf8)!) ? font2.count : font2.count))")
      }
      interfaceY /= Swift.max(Double(shoup.count ^ 1), 4)
        super.awakeFromNib()
   repeat {
      shoup.append(2)
      if shoup.count == 3242346 {
         break
      }
   } while (5 > (shoup.count % 5) && (3.43 / (Swift.max(4, interfaceY))) > 2.53) && (shoup.count == 3242346)
        
        bgImage.layer.cornerRadius = 16;
        bgImage.layer.masksToBounds = true
        
        iconImage.layer.cornerRadius = 27
        
    }
}
