
import Foundation

import UIKit

class CDetailsCell: UICollectionViewCell {
var hasReusable: Bool = false
var engineShuLevel_string: String!
private var about_padding: Double = 0.0
var table_max: Double = 0.0




    @IBOutlet weak var Icon: UIImageView!
    @IBOutlet weak var button: UIButton!

@discardableResult
 func aidStatusIndexSenderTableView(showTerminate: [String: Any]!, aymentHistory: Int) -> UIView! {
    var resumeZ: String! = String(cString: [114,101,97,114,0], encoding: .utf8)!
    var zoomJ: Bool = true
   for _ in 0 ..< 1 {
      zoomJ = resumeZ.count == 63
   }
      resumeZ = "\(1)"
   repeat {
      zoomJ = resumeZ.count > 13 || zoomJ
      if zoomJ ? !zoomJ : zoomJ {
         break
      }
   } while (!resumeZ.contains("\(zoomJ)")) && (zoomJ ? !zoomJ : zoomJ)
     var itemJiao: String! = String(cString: [114,101,115,105,103,110,115,0], encoding: .utf8)!
     let indexUnselected: UIButton! = UIButton()
     let statuesLong_9: String! = String(cString: [115,116,111,112,101,100,0], encoding: .utf8)!
    var subpacketsZconvolveJobq: UIView! = UIView(frame:CGRect(x: 305, y: 183, width: 0, height: 0))
    subpacketsZconvolveJobq.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    subpacketsZconvolveJobq.alpha = 0.2
    subpacketsZconvolveJobq.frame = CGRect(x: 63, y: 204, width: 0, height: 0)
    indexUnselected.frame = CGRect(x: 306, y: 299, width: 0, height: 0)
    indexUnselected.alpha = 0.4;
    indexUnselected.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    
    var indexUnselectedFrame = indexUnselected.frame
    indexUnselectedFrame.size = CGSize(width: 191, height: 164)
    indexUnselected.frame = indexUnselectedFrame
    if indexUnselected.alpha > 0.0 {
         indexUnselected.alpha = 0.0
    }
    if indexUnselected.isHidden {
         indexUnselected.isHidden = false
    }
    if !indexUnselected.isUserInteractionEnabled {
         indexUnselected.isUserInteractionEnabled = true
    }

    subpacketsZconvolveJobq.addSubview(indexUnselected)

    
    var subpacketsZconvolveJobqFrame = subpacketsZconvolveJobq.frame
    subpacketsZconvolveJobqFrame.size = CGSize(width: 90, height: 212)
    subpacketsZconvolveJobq.frame = subpacketsZconvolveJobqFrame
    if subpacketsZconvolveJobq.alpha > 0.0 {
         subpacketsZconvolveJobq.alpha = 0.0
    }
    if subpacketsZconvolveJobq.isHidden {
         subpacketsZconvolveJobq.isHidden = false
    }
    if !subpacketsZconvolveJobq.isUserInteractionEnabled {
         subpacketsZconvolveJobq.isUserInteractionEnabled = true
    }

    return subpacketsZconvolveJobq

}





    
    override func awakeFromNib() {

         let strkPublishers: UIView! = aidStatusIndexSenderTableView(showTerminate:[String(cString: [114,101,99,111,110,115,116,114,117,99,116,105,111,110,0], encoding: .utf8)!:[522, 915, 352]], aymentHistory:1126)

      if strkPublishers != nil {
          let strkPublishers_tag = strkPublishers.tag
          self.addSubview(strkPublishers)
      }
      else {
          print("strkPublishers is nil")      }



       var orderN: Int = 2
    var promptj: Bool = false
   for _ in 0 ..< 1 {
      orderN += (orderN ^ (promptj ? 2 : 3))
   }
      promptj = 87 < orderN || promptj

   if 4 >= (1 >> (Swift.min(1, labs(orderN)))) {
      orderN ^= 1
   }
      orderN %= Swift.max(2, 2)
        super.awakeFromNib()
        
        
    }

}
