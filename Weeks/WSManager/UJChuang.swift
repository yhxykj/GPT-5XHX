
import Foundation

import UIKit
import Speech
import AVFAudio
import AdSupport
import Accelerate
import SVProgressHUD

enum ZToolsM {
    case ZJiaoPopup
    case ZMineSearch
}

class UJChuang: NSObject {
private var imagesDesclabelDid_map: [String: Any]!
var controllerIndex: Int = 0
private var yloading_min: Double = 0.0
var delegate_7: Double = 0.0


    
    let utils: NeoNuiTts = NeoNuiTts.get_instance()
    let voicePlayer: IGraphics = IGraphics()
    typealias CompletionHandler = (ZToolsM) -> Void
    var completionHandler: CompletionHandler?
    
    static let shared: UJChuang = {
       var n_imageo: Double = 3.0
    var paintm: [String: Any]! = [String(cString: [109,97,108,101,0], encoding: .utf8)!:211, String(cString: [100,111,117,98,108,101,115,0], encoding: .utf8)!:690]
   if (n_imageo / (Swift.max(8, Double(paintm.values.count)))) >= 5.45 && (n_imageo / 5.45) >= 3.25 {
       var hengC: [String: Any]! = [String(cString: [110,111,98,111,100,121,0], encoding: .utf8)!:582, String(cString: [97,99,113,117,105,114,101,0], encoding: .utf8)!:820, String(cString: [107,98,105,116,0], encoding: .utf8)!:838]
       var placeholder1: String! = String(cString: [100,101,112,97,99,107,101,116,105,122,101,114,0], encoding: .utf8)!
       var executeF: String! = String(cString: [97,116,116,101,109,116,115,0], encoding: .utf8)!
       var briefW: [String: Any]! = [String(cString: [102,105,110,97,108,105,122,101,0], encoding: .utf8)!:String(cString: [101,120,105,115,116,97,110,99,101,0], encoding: .utf8)!, String(cString: [109,101,109,115,121,115,0], encoding: .utf8)!:String(cString: [99,104,101,99,107,115,117,109,115,0], encoding: .utf8)!, String(cString: [112,97,115,112,0], encoding: .utf8)!:String(cString: [97,118,103,121,0], encoding: .utf8)!]
       var sendB: [Any]! = [String(cString: [117,110,105,115,119,97,112,0], encoding: .utf8)!, String(cString: [119,105,100,103,101,116,0], encoding: .utf8)!]
         sendB = [2]
      repeat {
         executeF = "\(3 >> (Swift.min(3, sendB.count)))"
         if executeF == (String(cString:[112,49,117,105,118,114,110,109,105,52,0], encoding: .utf8)!) {
            break
         }
      } while (executeF == (String(cString:[112,49,117,105,118,114,110,109,105,52,0], encoding: .utf8)!)) && ((executeF.count & sendB.count) >= 2)
      while (sendB.count < executeF.count) {
          var zhidingese: String! = String(cString: [115,117,98,115,116,105,116,117,116,101,0], encoding: .utf8)!
          var headers1: String! = String(cString: [106,111,105,110,101,114,95,119,95,54,54,0], encoding: .utf8)!
          var awakea: String! = String(cString: [111,112,116,97,114,103,0], encoding: .utf8)!
          var speedZ: String! = String(cString: [114,101,112,108,121,0], encoding: .utf8)!
         sendB = [zhidingese.count]
         zhidingese = "\(3)"
         headers1 = "\(1)"
         awakea.append("\(awakea.count >> (Swift.min(labs(1), 3)))")
         speedZ = "\(3)"
         break
      }
         hengC[executeF] = hengC.keys.count & 2
          var enabledj: Float = 4.0
          var completez: [Any]! = [205, 580]
          var replaceB: Double = 0.0
         hengC = ["\(completez.count)": completez.count]
         enabledj -= Float(3)
         replaceB += Double(Int(enabledj))
      while (executeF.count <= hengC.keys.count) {
         executeF = "\(((String(cString:[104,0], encoding: .utf8)!) == placeholder1 ? briefW.keys.count : placeholder1.count))"
         break
      }
       var disconnecta: String! = String(cString: [110,101,101,100,101,100,0], encoding: .utf8)!
       var response6: String! = String(cString: [115,116,114,101,97,109,0], encoding: .utf8)!
          var sendb: String! = String(cString: [112,114,101,102,105,120,101,115,0], encoding: .utf8)!
          var labeelh: Float = 4.0
         executeF = "\(hengC.values.count)"
         sendb.append("\(sendb.count ^ Int(labeelh))")
         labeelh += Float(Int(labeelh))
      for _ in 0 ..< 1 {
          var data3: String! = String(cString: [109,111,111,102,0], encoding: .utf8)!
          var socketZ: Int = 2
          var recognizerR: [String: Any]! = [String(cString: [102,111,110,116,115,105,122,101,0], encoding: .utf8)!:8673.0]
          var strL: String! = String(cString: [112,114,111,109,105,115,101,0], encoding: .utf8)!
         response6.append("\(2 | strL.count)")
         data3.append("\(3)")
         socketZ &= 2
         recognizerR[data3] = socketZ
         strL.append("\(recognizerR.values.count | 1)")
      }
         hengC["\(sendB.count)"] = 3 & sendB.count
      for _ in 0 ..< 1 {
         response6 = "\(2)"
      }
         sendB.append(executeF.count)
         disconnecta = "\(3)"
         executeF.append("\(placeholder1.count << (Swift.min(labs(3), 1)))")
         sendB.append(hengC.keys.count - 1)
      paintm = ["\(briefW.count)": briefW.keys.count]
   }

        let instance = UJChuang()
   for _ in 0 ..< 2 {
       var label1: Double = 3.0
       var detaillabelr: String! = String(cString: [100,117,97,108,0], encoding: .utf8)!
       var albumZ: String! = String(cString: [101,110,116,114,111,112,121,109,118,0], encoding: .utf8)!
       var alabelR: String! = String(cString: [109,118,101,120,0], encoding: .utf8)!
       var enabledi: String! = String(cString: [98,105,103,0], encoding: .utf8)!
         alabelR.append("\(alabelR.count ^ 2)")
       var instanceg: [Any]! = [[String(cString: [97,118,99,99,0], encoding: .utf8)!:String(cString: [97,97,99,115,98,114,100,97,116,97,0], encoding: .utf8)!, String(cString: [115,104,97,112,101,0], encoding: .utf8)!:String(cString: [118,112,99,99,0], encoding: .utf8)!, String(cString: [115,99,114,101,101,110,115,0], encoding: .utf8)!:String(cString: [115,101,97,114,99,104,105,110,103,0], encoding: .utf8)!]]
      for _ in 0 ..< 3 {
         instanceg.append((detaillabelr == (String(cString:[98,0], encoding: .utf8)!) ? detaillabelr.count : Int(label1)))
      }
         detaillabelr.append("\((detaillabelr == (String(cString:[84,0], encoding: .utf8)!) ? detaillabelr.count : instanceg.count))")
         albumZ.append("\(albumZ.count << (Swift.min(3, instanceg.count)))")
      repeat {
          var validate6: [Any]! = [81, 473, 576]
          var scaleN: [Any]! = [56, 475]
         instanceg.append(1 | scaleN.count)
         validate6 = [3 - validate6.count]
         scaleN = [validate6.count]
         if 923803 == instanceg.count {
            break
         }
      } while (923803 == instanceg.count) && (3 == (5 % (Swift.max(9, albumZ.count))))
      if 2 <= (instanceg.count + 5) && (instanceg.count + 5) <= 1 {
          var ylabelp: String! = String(cString: [97,114,109,111,110,121,0], encoding: .utf8)!
          var k_playern: [String: Any]! = [String(cString: [99,104,97,112,116,101,114,115,0], encoding: .utf8)!:6268]
          var audioV: String! = String(cString: [115,117,112,101,114,115,101,116,0], encoding: .utf8)!
          var meald: Double = 2.0
          var s_viewW: String! = String(cString: [102,105,102,111,115,0], encoding: .utf8)!
         enabledi.append("\(instanceg.count % (Swift.max(detaillabelr.count, 1)))")
         ylabelp.append("\(Int(meald) - k_playern.values.count)")
         k_playern = ["\(k_playern.count)": 1 ^ k_playern.values.count]
         audioV.append("\(s_viewW.count)")
         meald -= (Double((String(cString:[108,0], encoding: .utf8)!) == ylabelp ? s_viewW.count : ylabelp.count))
      }
         detaillabelr.append("\(alabelR.count ^ albumZ.count)")
      while ((instanceg.count / 2) > 5) {
         instanceg.append(albumZ.count / (Swift.max(5, detaillabelr.count)))
         break
      }
      if !enabledi.hasPrefix(albumZ) {
          var y_countq: String! = String(cString: [112,101,114,115,112,101,99,116,105,118,101,0], encoding: .utf8)!
          var update_v9e: Double = 1.0
          var verticalO: String! = String(cString: [98,111,117,110,100,101,100,0], encoding: .utf8)!
          var rmblabel9: [Any]! = [786, 373]
          var menuI: [Any]! = [152, 274, 694]
         albumZ = "\(alabelR.count & Int(label1))"
         y_countq.append("\(1)")
         update_v9e += Double(1)
         verticalO.append("\(verticalO.count)")
         rmblabel9 = [Int(update_v9e) - verticalO.count]
         menuI = [Int(update_v9e) / (Swift.max(7, y_countq.count))]
      }
          var list1: String! = String(cString: [116,101,97,109,115,0], encoding: .utf8)!
         instanceg = [instanceg.count]
         list1.append("\(2 * list1.count)")
          var versionw: Bool = false
          var not_fk7: [String: Any]! = [String(cString: [117,110,97,108,105,103,110,0], encoding: .utf8)!:25, String(cString: [116,111,114,99,104,0], encoding: .utf8)!:933]
          var login_: Int = 2
         albumZ.append("\(albumZ.count / (Swift.max(1, 10)))")
         versionw = not_fk7.count > 31 || login_ > 31
         not_fk7 = ["\(not_fk7.count)": not_fk7.values.count * 2]
         login_ |= 1
      while (5 > alabelR.count) {
         detaillabelr.append("\(Int(label1))")
         break
      }
         label1 /= Swift.max(2, Double(albumZ.count))
      while (label1 >= 3.76) {
         detaillabelr.append("\(alabelR.count)")
         break
      }
      n_imageo *= Double(enabledi.count % 2)
   }
        instance.utils.delegate = instance
   while (!paintm.values.contains { $0 as? Double == n_imageo }) {
      n_imageo /= Swift.max(Double(Int(n_imageo) ^ paintm.keys.count), 2)
      break
   }
        instance.voicePlayer.delegate = instance
   repeat {
       var silenceg: Double = 1.0
       var back0: [String: Any]! = [String(cString: [97,117,100,0], encoding: .utf8)!:971, String(cString: [98,105,116,114,118,99,111,110,106,0], encoding: .utf8)!:722]
       var statues8: Bool = false
       var observationsC: Double = 1.0
       var aymentI: [String: Any]! = [String(cString: [112,114,101,105,118,111,117,115,0], encoding: .utf8)!:577, String(cString: [102,111,115,115,105,108,0], encoding: .utf8)!:813]
         statues8 = (Double(back0.count) / (Swift.max(7, observationsC))) < 61.46
       var resourcesS: Int = 2
       var bufferO: Int = 3
         bufferO %= Swift.max(3 & resourcesS, 5)
          var true_0n: String! = String(cString: [105,115,115,117,101,100,0], encoding: .utf8)!
          var hasT: String! = String(cString: [116,108,101,110,0], encoding: .utf8)!
          var datasv: Float = 4.0
         aymentI["\(statues8)"] = resourcesS
         true_0n.append("\(1 >> (Swift.min(2, true_0n.count)))")
         hasT.append("\(Int(datasv) + hasT.count)")
         datasv += Float(hasT.count)
      repeat {
         statues8 = observationsC == 28.100
         if statues8 ? !statues8 : statues8 {
            break
         }
      } while (resourcesS < 1 || (resourcesS | 1) < 5) && (statues8 ? !statues8 : statues8)
      while (2 >= (5 + resourcesS)) {
         resourcesS *= 3 ^ Int(observationsC)
         break
      }
      if 2.67 < observationsC {
         statues8 = aymentI["\(bufferO)"] != nil
      }
      while (3 < (resourcesS % (Swift.max(2, aymentI.values.count))) || 3 < (resourcesS % (Swift.max(aymentI.values.count, 3)))) {
         resourcesS += Int(observationsC) | aymentI.values.count
         break
      }
         back0["\(resourcesS)"] = 1 << (Swift.min(4, aymentI.count))
         observationsC /= Swift.max(Double(Int(silenceg) % 3), 2)
      for _ in 0 ..< 3 {
         aymentI["\(statues8)"] = 2
      }
      if 5 <= (bufferO * 1) {
         bufferO ^= 3
      }
         bufferO *= 3
      repeat {
          var reusableR: Int = 5
          var section_: Int = 5
         silenceg /= Swift.max(Double(bufferO | 2), 3)
         reusableR += section_
         if 1509219.0 == silenceg {
            break
         }
      } while ((silenceg / (Swift.max(6, observationsC))) >= 3.72 && 5.88 >= (3.72 + silenceg)) && (1509219.0 == silenceg)
         observationsC *= Double(2)
      paintm["\(observationsC)"] = back0.count
      if paintm.count == 3904839 {
         break
      }
   } while (paintm.count == 3904839) && (paintm["\(n_imageo)"] != nil)
        return instance
    }()

@discardableResult
 func backgroundGravityScreenAnswer(desclabelViptimelabel: Float, detailsLike: Double, recognitionRequest: [Any]!) -> Double {
    var launch5: Int = 1
    var executeO: Bool = false
       var serviceV: String! = String(cString: [107,98,112,115,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
          var headers0: Float = 5.0
          var downloadu: String! = String(cString: [104,109,109,116,0], encoding: .utf8)!
          var codeS: [String: Any]! = [String(cString: [105,110,108,105,110,107,0], encoding: .utf8)!:732, String(cString: [108,105,98,97,118,102,111,114,109,97,116,0], encoding: .utf8)!:658]
          var controllerA: [Any]! = [169, 705]
         serviceV.append("\(((String(cString:[120,0], encoding: .utf8)!) == serviceV ? Int(headers0) : serviceV.count))")
         headers0 -= Float(controllerA.count)
         downloadu.append("\(downloadu.count)")
         codeS = ["\(codeS.count)": 1]
         controllerA = [3]
      }
      if !serviceV.contains(serviceV) {
         serviceV = "\(serviceV.count >> (Swift.min(4, serviceV.count)))"
      }
          var evetl: String! = String(cString: [112,114,111,112,111,115,101,100,0], encoding: .utf8)!
         serviceV = "\(evetl.count)"
      executeO = serviceV.count > 62 || !executeO
       var generatej: [Any]! = [382, 454, 929]
       var shared0: Bool = true
       var info1: Double = 4.0
       var self_rX: String! = String(cString: [118,99,111,119,112,116,114,0], encoding: .utf8)!
         shared0 = !shared0
          var relationw: String! = String(cString: [100,101,115,116,114,117,99,116,111,114,95,103,95,52,55,0], encoding: .utf8)!
          var sectionG: Int = 1
         info1 *= Double(3 ^ generatej.count)
         relationw.append("\(2 & sectionG)")
         sectionG *= ((String(cString:[78,0], encoding: .utf8)!) == relationw ? sectionG : relationw.count)
         self_rX = "\(2 << (Swift.min(labs(Int(info1)), 3)))"
         info1 *= Double(3 ^ self_rX.count)
       var popup6: String! = String(cString: [101,110,116,105,116,121,0], encoding: .utf8)!
         self_rX = "\(generatej.count)"
         generatej = [3 << (Swift.min(4, self_rX.count))]
         info1 += Double(self_rX.count << (Swift.min(1, generatej.count)))
      executeO = executeO && shared0
      executeO = launch5 >= 97 || !executeO
      executeO = !executeO || 61 < launch5
     let jiaoQueue: Double = 6796.0
    var secureFramepool:Double = 0
    secureFramepool -= jiaoQueue

    return secureFramepool

}





    
    func startPlay(fontName: String = "", message: String, completionHandler: CompletionHandler?) {

             backgroundGravityScreenAnswer(desclabelViptimelabel:6988.0, detailsLike:6217.0, recognitionRequest:[false])


       var closev: Float = 1.0
    var detail4: Int = 4
      detail4 >>= Swift.min(labs(1 | detail4), 3)

   if (Double(detail4 / (Swift.max(Int(closev), 5)))) <= 4.40 {
       var totalA: String! = String(cString: [100,101,99,111,109,112,111,115,101,0], encoding: .utf8)!
       var amounti: Double = 5.0
       var goodsY: String! = String(cString: [112,114,115,99,116,112,0], encoding: .utf8)!
       var detailsu: Bool = false
       var timelabell: String! = String(cString: [112,99,105,112,104,101,114,116,101,120,116,0], encoding: .utf8)!
      while (totalA.count <= timelabell.count) {
          var logoQ: Double = 4.0
          var jiaoc: Int = 3
          var fromf: String! = String(cString: [109,101,115,115,97,103,101,115,0], encoding: .utf8)!
          var back6: String! = String(cString: [101,120,112,105,114,97,116,105,111,110,115,0], encoding: .utf8)!
         timelabell = "\(3)"
         logoQ += Double(jiaoc * 1)
         jiaoc -= 2
         fromf = "\(back6.count)"
         back6 = "\(3 << (Swift.min(1, fromf.count)))"
         break
      }
      while (2 <= goodsY.count) {
          var images7: Double = 5.0
          var volumet: [String: Any]! = [String(cString: [114,101,102,105,108,108,0], encoding: .utf8)!:289, String(cString: [99,108,101,97,114,118,105,100,101,111,100,97,116,97,0], encoding: .utf8)!:325]
          var edit1: Double = 1.0
         goodsY = "\(2 ^ goodsY.count)"
         images7 *= Double(3)
         volumet["\(edit1)"] = 3
         edit1 /= Swift.max(Double(1 & Int(images7)), 2)
         break
      }
       var formatterX: String! = String(cString: [112,108,117,103,105,110,0], encoding: .utf8)!
         amounti *= Double(2 >> (Swift.min(labs(Int(amounti)), 4)))
          var promptD: String! = String(cString: [100,101,108,115,117,112,101,114,0], encoding: .utf8)!
         formatterX = "\(2 - promptD.count)"
         totalA = "\(formatterX.count)"
         detailsu = totalA.contains("\(amounti)")
          var titlelabeld: Int = 3
          var arrayc: String! = String(cString: [98,117,102,108,101,110,0], encoding: .utf8)!
         detailsu = 40 > titlelabeld
         titlelabeld ^= arrayc.count >> (Swift.min(labs(3), 2))
         arrayc.append("\(arrayc.count >> (Swift.min(1, arrayc.count)))")
      for _ in 0 ..< 1 {
         formatterX = "\(2)"
      }
         formatterX.append("\(3)")
      while (3 >= totalA.count) {
          var fast7: Double = 3.0
          var y_widthN: Double = 4.0
          var versionlabela: Bool = true
          var loginb: Bool = false
         detailsu = versionlabela || goodsY.count == 59
         fast7 += (Double((loginb ? 1 : 2) << (Swift.min(labs(Int(y_widthN)), 4))))
         y_widthN *= (Double(Int(y_widthN) << (Swift.min(4, labs((loginb ? 4 : 1))))))
         versionlabela = 38.4 > y_widthN && loginb
         break
      }
       var formatterI: String! = String(cString: [97,112,112,114,111,118,101,0], encoding: .utf8)!
       var bangL: String! = String(cString: [100,99,111,110,108,121,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
         formatterI.append("\(2)")
      }
      repeat {
         detailsu = (String(cString:[101,0], encoding: .utf8)!) == bangL
         if detailsu ? !detailsu : detailsu {
            break
         }
      } while (detailsu ? !detailsu : detailsu) && (detailsu || 5.46 >= (2.55 + amounti))
      for _ in 0 ..< 1 {
          var urlE: String! = String(cString: [109,105,108,108,105,115,0], encoding: .utf8)!
          var bundleB: Int = 4
          var expire3: String! = String(cString: [108,101,97,115,116,0], encoding: .utf8)!
         totalA.append("\(((String(cString:[106,0], encoding: .utf8)!) == totalA ? bundleB : totalA.count))")
         urlE.append("\(expire3.count)")
         bundleB %= Swift.max(5, (urlE == (String(cString:[49,0], encoding: .utf8)!) ? urlE.count : expire3.count))
      }
      detail4 ^= 3
   }
        
        stopPlay(false)
        
        DispatchQueue.main.asyncAfter(deadline: .now() + 0.3) { [self]  in
   for _ in 0 ..< 1 {
      detail4 += Int(closev) | 2
   }
            
            self.completionHandler = completionHandler
            
            utils.nui_tts_initialize(getInitParam(), logLevel: NuiSdkLogLevel(0), saveLog: true)

            if fontName == "" {
                if let font_name: String = UserDefaults.standard.object(forKey: "font_name") as? String {
                    utils.nui_tts_set_param("font_name", value: font_name)
                }else {
                    utils.nui_tts_set_param("font_name", value: "zhimiao_emo")
                }
            }else {
                utils.nui_tts_set_param("font_name", value: fontName)
            }
            let attributesImage = UserDefaults.standard.float(forKey: "rate")
            if attributesImage > 0 {
                utils.nui_tts_set_param("speed_level", value: String(format: "%0.2f", attributesImage))
            }
            utils.nui_tts_play("1", taskId: "", text: message)
            
        }
    }

    
    func stopPlay(_ isBlock: Bool = true) {
       var detailsO: String! = String(cString: [116,105,109,101,105,110,116,101,114,118,97,108,0], encoding: .utf8)!
    var reusablem: String! = String(cString: [108,105,98,99,0], encoding: .utf8)!
   if detailsO.hasPrefix(reusablem) {
      reusablem.append("\(((String(cString:[79,0], encoding: .utf8)!) == detailsO ? reusablem.count : detailsO.count))")
   }

   while (2 == detailsO.count || reusablem != String(cString:[90,0], encoding: .utf8)!) {
       var collateK: Float = 5.0
       var collatec: String! = String(cString: [101,116,104,114,101,97,100,105,110,103,0], encoding: .utf8)!
       var sourceC: Double = 2.0
       var number6: String! = String(cString: [98,117,102,115,112,97,99,101,0], encoding: .utf8)!
       var amounto: String! = String(cString: [120,108,97,98,101,108,119,105,100,116,104,0], encoding: .utf8)!
         collatec = "\(((String(cString:[100,0], encoding: .utf8)!) == number6 ? Int(collateK) : number6.count))"
      if 3 >= (Int(collateK) / (Swift.max(number6.count, 7))) || 3 >= (Int(collateK) / (Swift.max(number6.count, 6))) {
         number6 = "\(amounto.count * number6.count)"
      }
          var statusC: String! = String(cString: [114,101,116,97,105,110,101,100,0], encoding: .utf8)!
          var descriptC: [Any]! = [451, 800]
         sourceC /= Swift.max(1, (Double(statusC == (String(cString:[118,0], encoding: .utf8)!) ? number6.count : statusC.count)))
         descriptC = [1]
       var numberlabelA: Int = 1
          var contextb: String! = String(cString: [102,98,100,101,118,0], encoding: .utf8)!
         collateK *= (Float((String(cString:[56,0], encoding: .utf8)!) == number6 ? Int(sourceC) : number6.count))
         contextb = "\(((String(cString:[69,0], encoding: .utf8)!) == contextb ? contextb.count : contextb.count))"
      while ((Double(amounto.count) * sourceC) == 5.80 || 2.11 == (sourceC * 5.80)) {
         sourceC /= Swift.max(2, Double(numberlabelA))
         break
      }
      for _ in 0 ..< 3 {
          var recordingP: String! = String(cString: [104,99,104,114,111,109,97,0], encoding: .utf8)!
          var phoneG: String! = String(cString: [104,111,110,101,0], encoding: .utf8)!
         sourceC /= Swift.max(Double(numberlabelA), 5)
         recordingP.append("\(phoneG.count >> (Swift.min(labs(3), 2)))")
         phoneG = "\(phoneG.count + recordingP.count)"
      }
      detailsO.append("\(2)")
      break
   }
        
        voicePlayer.stop()
      reusablem.append("\(3 * reusablem.count)")
        utils.nui_tts_cancel(nil)
        if isBlock { self.completionHandler?(.ZMineSearch) }
        
    }
}

func createPath() -> String {
       var editL: String! = String(cString: [104,112,105,99,0], encoding: .utf8)!
    var befores: [String: Any]! = [String(cString: [109,111,100,109,0], encoding: .utf8)!:684, String(cString: [114,101,106,101,99,116,0], encoding: .utf8)!:667, String(cString: [115,105,110,113,98,0], encoding: .utf8)!:25]
       var speedst: Int = 2
       var candidateu: [Any]! = [631, 493]
         speedst >>= Swift.min(4, labs(candidateu.count >> (Swift.min(3, labs(speedst)))))
          var collectionG: Double = 5.0
         speedst &= candidateu.count | Int(collectionG)
      if (candidateu.count ^ speedst) == 4 && 3 == (4 ^ candidateu.count) {
          var levels: String! = String(cString: [105,110,105,116,105,97,108,105,122,101,114,115,0], encoding: .utf8)!
          var recordingv8: Double = 3.0
          var sheett: String! = String(cString: [99,101,108,112,0], encoding: .utf8)!
          var current4: String! = String(cString: [112,97,114,97,98,111,108,108,105,99,0], encoding: .utf8)!
         speedst -= 3 + speedst
         levels.append("\(1 >> (Swift.min(5, labs(Int(recordingv8)))))")
         recordingv8 *= (Double(levels == (String(cString:[119,0], encoding: .utf8)!) ? Int(recordingv8) : levels.count))
         sheett.append("\(Int(recordingv8) >> (Swift.min(labs(1), 3)))")
         current4 = "\(sheett.count + 2)"
      }
          var rowN: Int = 4
          var titlelabelm: [String: Any]! = [String(cString: [116,111,111,108,116,105,112,0], encoding: .utf8)!:187, String(cString: [111,99,115,112,105,100,0], encoding: .utf8)!:490]
          var cello: Double = 0.0
         candidateu.append(speedst)
         rowN %= Swift.max(rowN << (Swift.min(1, labs(1))), 1)
         titlelabelm["\(cello)"] = Int(cello) & 2
         candidateu = [speedst - 3]
         speedst += speedst ^ candidateu.count
      befores[editL] = candidateu.count

   while (5 <= (editL.count * 2)) {
      befores = ["\(befores.count)": editL.count]
      break
   }
       
    let unselected = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true)
   repeat {
      editL.append("\(befores.count - editL.count)")
      if (String(cString:[50,98,122,56,52,116,121,114,52,118,0], encoding: .utf8)!) == editL {
         break
      }
   } while (editL.count <= befores.values.count) && ((String(cString:[50,98,122,56,52,116,121,114,52,118,0], encoding: .utf8)!) == editL)
    let slider = unselected[0]
   
    let call = FileManager.default
    let phoneCel = (slider as NSString).appendingPathComponent("voices")
    
    do {
        try call.createDirectory(atPath: phoneCel, withIntermediateDirectories: true, attributes: nil)
        print("文件夹创建成功")
    } catch {
        print("文件夹创建失败")
    }
    return phoneCel
}

func getInitParam() -> String {
       var recognizedP: [String: Any]! = [String(cString: [97,109,101,120,0], encoding: .utf8)!:[58, 232]]
    var mark3: String! = String(cString: [115,116,114,101,97,109,99,111,112,121,0], encoding: .utf8)!
    var clearO: Float = 2.0
   for _ in 0 ..< 1 {
      recognizedP["\(mark3)"] = 2
   }

       
    guard let strResourcesBundle = Bundle.main.path(forResource: "Resources", ofType: "bundle") else {
        return ""
    }
    
    let free = Bundle(path: strResourcesBundle)?.resourcePath ?? ""
   for _ in 0 ..< 1 {
      clearO += (Float((String(cString:[67,0], encoding: .utf8)!) == mark3 ? recognizedP.keys.count : mark3.count))
   }
    let heng = ASIdentifierManager.shared().advertisingIdentifier.uuidString
 
    let headers = createPath()
    
    if let token = UserDefaults.standard.string(forKey: "AliToken") {
        
        var labeel = [String: Any]()
        labeel["app_key"] = "FwsOLV8DKcHopkcl"
        labeel["token"] = token
        labeel["workspace"] = free
        labeel["debug_path"] = headers
        labeel["device_id"] = heng
        labeel["url"] = "wss://nls-gateway.cn-shanghai.aliyuncs.com:443/ws/v1"
        labeel["mode_type"] = "2"

        do {
            let avatar = try JSONSerialization.data(withJSONObject: labeel, options: .prettyPrinted)
            if let jsonStr = String(data: avatar, encoding: .utf8) {
                return jsonStr
            }
        } catch {
            print("JSONSerialization error: \(error)")
        }
    }else {
        SVProgressHUD.showError(withStatus: "阿里Token无效")
    }

    return ""
}

func checkAliToken() {
       var dataN: String! = String(cString: [109,112,108,97,109,101,0], encoding: .utf8)!
    var addressS: [Any]! = [753, 694, 25]
   if addressS.count <= dataN.count {
      addressS = [dataN.count]
   }

    
    GMIToolsOther.shared.post(urlSuffix: "/app/getAliyunToken") { (result: Result<IIMenu, ULogint>) in
   for _ in 0 ..< 2 {
       var alamofires: String! = String(cString: [116,97,103,115,0], encoding: .utf8)!
       var audioU: Double = 4.0
       var openk: String! = String(cString: [100,101,116,97,99,104,0], encoding: .utf8)!
       var lishiq: Int = 3
       var meali: String! = String(cString: [102,108,111,97,116,105,110,103,0], encoding: .utf8)!
         meali = "\(meali.count)"
      repeat {
         meali = "\(meali.count)"
         if (String(cString:[117,100,56,111,0], encoding: .utf8)!) == meali {
            break
         }
      } while ((String(cString:[117,100,56,111,0], encoding: .utf8)!) == meali) && ((meali.count / (Swift.max(4, lishiq))) >= 4 && (lishiq / (Swift.max(4, 3))) >= 2)
          var graphicsD: Double = 3.0
         lishiq += 2 & openk.count
         graphicsD -= Double(3)
      for _ in 0 ..< 1 {
          var tapq: String! = String(cString: [116,114,97,99,107,115,0], encoding: .utf8)!
          var datew: String! = String(cString: [105,102,105,108,116,101,114,0], encoding: .utf8)!
         openk = "\(Int(audioU) ^ 2)"
         tapq = "\(tapq.count)"
         datew.append("\(datew.count / (Swift.max(tapq.count, 10)))")
      }
         audioU /= Swift.max(1, (Double((String(cString:[118,0], encoding: .utf8)!) == alamofires ? alamofires.count : Int(audioU))))
      repeat {
         openk.append("\(((String(cString:[70,0], encoding: .utf8)!) == openk ? alamofires.count : openk.count))")
         if openk == (String(cString:[48,50,49,0], encoding: .utf8)!) {
            break
         }
      } while (openk == (String(cString:[48,50,49,0], encoding: .utf8)!)) && (1 >= (lishiq - 5) && (lishiq - openk.count) >= 5)
      repeat {
         lishiq += Int(audioU) * meali.count
         if 4102054 == lishiq {
            break
         }
      } while ((alamofires.count >> (Swift.min(labs(5), 1))) < 3 && (5 >> (Swift.min(1, alamofires.count))) < 5) && (4102054 == lishiq)
       var buttond: Bool = true
       var nameY: Bool = false
         alamofires.append("\(((String(cString:[49,0], encoding: .utf8)!) == openk ? Int(audioU) : openk.count))")
      while (2 >= (4 % (Swift.max(5, lishiq))) && !buttond) {
         buttond = alamofires.count < 21
         break
      }
      if alamofires.count > 4 {
         openk.append("\(meali.count)")
      }
      repeat {
          var formatterp: [Any]! = [415, 881, 876]
          var l_managero: String! = String(cString: [103,101,116,110,112,97,115,115,101,115,0], encoding: .utf8)!
         meali = "\(lishiq)"
         formatterp.append(formatterp.count % 2)
         l_managero.append("\(3 >> (Swift.min(5, formatterp.count)))")
         if meali == (String(cString:[117,122,118,119,54,113,0], encoding: .utf8)!) {
            break
         }
      } while (openk.contains("\(meali.count)")) && (meali == (String(cString:[117,122,118,119,54,113,0], encoding: .utf8)!))
         lishiq %= Swift.max(2, alamofires.count)
       var editO: Bool = true
      for _ in 0 ..< 2 {
         buttond = (!buttond ? !nameY : !buttond)
      }
      addressS = [meali.count * alamofires.count]
   }
        switch result {
        case .success(let model):
            if model.code == 200 {
                UserDefaults.standard.setValue(model.data, forKey: "AliToken")
   for _ in 0 ..< 1 {
      addressS.append(addressS.count & dataN.count)
   }
                print("阿里Token\(model.data)")
      addressS.append(addressS.count)
                UserDefaults.standard.synchronize()
            }
            
            break
        case.failure(_):
            SVProgressHUD.showError(withStatus: "获取Token失败");
            break
        }
    }

}


extension UJChuang: NeoNuiTtsDelegate, HJMVKPopup {

@discardableResult
 func navigationKeyboardComponent(removeSelf_q: String!, logoReplace: Bool, animationAdd: [String: Any]!) -> Bool {
    var paintm: String! = String(cString: [111,117,98,108,101,0], encoding: .utf8)!
    var addressD: String! = String(cString: [110,97,110,111,112,98,95,121,95,51,49,0], encoding: .utf8)!
   while (2 > paintm.count) {
       var iosb: String! = String(cString: [103,109,97,116,99,104,0], encoding: .utf8)!
         iosb.append("\(iosb.count << (Swift.min(labs(3), 2)))")
         iosb = "\(iosb.count ^ 2)"
         iosb.append("\(2 / (Swift.max(8, iosb.count)))")
      addressD = "\((paintm == (String(cString:[122,0], encoding: .utf8)!) ? iosb.count : paintm.count))"
      break
   }
   for _ in 0 ..< 3 {
      paintm.append("\(3)")
   }
      paintm.append("\(paintm.count)")
   for _ in 0 ..< 3 {
       var candidatel: String! = String(cString: [105,110,116,101,114,108,101,97,118,101,100,0], encoding: .utf8)!
       var displayp: String! = String(cString: [97,99,113,117,97,110,116,0], encoding: .utf8)!
         candidatel.append("\(2 ^ displayp.count)")
      if candidatel.count <= displayp.count {
          var picS: Double = 5.0
          var editY: Bool = true
          var menuO: [String: Any]! = [String(cString: [99,105,112,104,101,114,116,101,120,116,0], encoding: .utf8)!:String(cString: [111,95,55,52,95,114,115,112,0], encoding: .utf8)!, String(cString: [115,95,50,54,95,109,117,108,116,105,115,105,103,0], encoding: .utf8)!:String(cString: [99,104,111,111,115,101,114,95,97,95,53,0], encoding: .utf8)!, String(cString: [110,111,116,99,104,0], encoding: .utf8)!:String(cString: [104,97,110,103,0], encoding: .utf8)!]
          var pasteboardd: [Any]! = [4252.0]
          var rmbo: String! = String(cString: [100,114,105,118,101,110,0], encoding: .utf8)!
         displayp.append("\(rmbo.count)")
         picS -= Double(menuO.values.count / (Swift.max(1, 9)))
         editY = pasteboardd.count < Int(picS)
         menuO["\(picS)"] = menuO.values.count << (Swift.min(labs(2), 2))
         pasteboardd.append(1)
         rmbo = "\(Int(picS) / (Swift.max(pasteboardd.count, 4)))"
      }
      for _ in 0 ..< 2 {
          var datasd: Double = 4.0
          var tap9: Int = 4
          var handlera: String! = String(cString: [105,100,108,101,95,106,95,56,57,0], encoding: .utf8)!
         displayp = "\(candidatel.count)"
         datasd -= Double(Int(datasd))
         tap9 %= Swift.max((handlera == (String(cString:[115,0], encoding: .utf8)!) ? handlera.count : tap9), 2)
      }
      if candidatel.contains(displayp) {
         displayp.append("\(2)")
      }
      repeat {
         candidatel = "\(candidatel.count)"
         if candidatel == (String(cString:[48,99,51,119,0], encoding: .utf8)!) {
            break
         }
      } while (5 == displayp.count) && (candidatel == (String(cString:[48,99,51,119,0], encoding: .utf8)!))
          var safeK: [Any]! = [778, 145, 487]
         candidatel = "\(2)"
         safeK = [3 * safeK.count]
      paintm.append("\(1)")
   }
     let pressChange: Int = 454
    var  explainCanvas:Bool = false
    explainCanvas = pressChange > 18

    return explainCanvas

}





    
    func onNuiTtsUserdataCallback(_ info: UnsafeMutablePointer<CChar>!, infoLen info_len: Int32, buffer: UnsafeMutablePointer<CChar>!, len: Int32, taskId task_id: UnsafeMutablePointer<CChar>!) {

             navigationKeyboardComponent(removeSelf_q:String(cString: [109,101,114,103,101,97,98,108,101,0], encoding: .utf8)!, logoReplace:true, animationAdd:[String(cString: [102,95,51,56,95,102,111,108,108,111,119,101,114,115,0], encoding: .utf8)!:320, String(cString: [100,108,105,100,120,0], encoding: .utf8)!:633, String(cString: [98,95,50,52,95,107,100,102,0], encoding: .utf8)!:603])


       var documentM: Int = 0
    var candidateC: String! = String(cString: [105,109,109,101,100,105,97,116,101,115,105,103,110,97,108,0], encoding: .utf8)!
       var launchD: [Any]! = [422]
       var ylabelB: Bool = true
          var itemse: String! = String(cString: [114,105,110,103,0], encoding: .utf8)!
          var relationL: String! = String(cString: [106,105,103,103,108,101,0], encoding: .utf8)!
          var preferredE: Bool = true
         ylabelB = (launchD.contains { $0 as? Bool == ylabelB })
         itemse.append("\((relationL == (String(cString:[69,0], encoding: .utf8)!) ? (preferredE ? 3 : 4) : relationL.count))")
         preferredE = !preferredE
         launchD.append(launchD.count)
         launchD.append(launchD.count)
       var taskH: Double = 4.0
       var menul: Double = 2.0
      for _ in 0 ..< 2 {
          var collectionL: String! = String(cString: [115,101,116,100,97,114,0], encoding: .utf8)!
          var find9: Float = 0.0
          var font4: String! = String(cString: [98,117,110,100,108,101,0], encoding: .utf8)!
          var generatorZ: String! = String(cString: [112,104,121,115,105,99,97,108,0], encoding: .utf8)!
         ylabelB = 100.60 > (find9 - Float(taskH))
         collectionL.append("\(font4.count)")
         find9 += Float(generatorZ.count * 1)
         font4 = "\(font4.count)"
         generatorZ.append("\(font4.count + 3)")
      }
         menul -= Double(1 / (Swift.max(Int(taskH), 8)))
      documentM |= 1 * candidateC.count

      documentM ^= candidateC.count & documentM

      candidateC = "\(candidateC.count)"
        if len > 0 {
            voicePlayer.write(buffer, length: Int32(len))
        }
       
      candidateC.append("\(documentM)")
    }

@discardableResult
 func navigationVerticallyChannelUnique(pasteboardPaint: Double, valuelabelAnswer: Bool, timerHandle: [String: Any]!) -> [Any]! {
    var flowN: Bool = false
    var recordingvP: String! = String(cString: [115,116,111,114,97,103,101,115,0], encoding: .utf8)!
   repeat {
      flowN = !flowN
      if flowN ? !flowN : flowN {
         break
      }
   } while (4 < recordingvP.count) && (flowN ? !flowN : flowN)
      recordingvP.append("\((recordingvP == (String(cString:[118,0], encoding: .utf8)!) ? recordingvP.count : (flowN ? 5 : 2)))")
       var register_g4: String! = String(cString: [113,95,54,49,95,98,101,103,117,110,0], encoding: .utf8)!
       var parameterd: String! = String(cString: [104,97,110,100,111,102,102,0], encoding: .utf8)!
       var r_widthw: String! = String(cString: [110,95,52,51,95,97,100,118,97,110,99,101,0], encoding: .utf8)!
          var recognizedD: String! = String(cString: [115,113,108,105,116,101,99,104,97,110,103,101,115,101,116,95,100,95,51,55,0], encoding: .utf8)!
          var tableQ: String! = String(cString: [107,109,115,103,114,97,98,0], encoding: .utf8)!
          var uploadd: [String: Any]! = [String(cString: [111,95,49,48,48,95,105,110,118,111,107,101,0], encoding: .utf8)!:7, String(cString: [99,111,109,109,97,110,100,108,105,110,101,102,108,97,103,0], encoding: .utf8)!:331]
         r_widthw = "\((tableQ == (String(cString:[102,0], encoding: .utf8)!) ? tableQ.count : uploadd.values.count))"
         recognizedD = "\(((String(cString:[106,0], encoding: .utf8)!) == recognizedD ? recognizedD.count : recognizedD.count))"
      for _ in 0 ..< 1 {
          var containsl: Int = 5
          var w_centerO: String! = String(cString: [116,114,97,105,110,95,121,95,57,57,0], encoding: .utf8)!
          var jsonO: String! = String(cString: [115,99,97,116,116,101,114,0], encoding: .utf8)!
          var areac: Bool = true
          var bufferE: [String: Any]! = [String(cString: [115,105,109,112,108,101,114,101,97,100,95,107,95,49,57,0], encoding: .utf8)!:189, String(cString: [109,109,120,101,120,116,0], encoding: .utf8)!:568, String(cString: [105,108,108,101,103,97,108,95,117,95,50,49,0], encoding: .utf8)!:224]
         parameterd.append("\(register_g4.count & w_centerO.count)")
         containsl <<= Swift.min(1, labs(2))
         w_centerO = "\(2)"
         jsonO = "\(jsonO.count + 3)"
         bufferE = ["\(bufferE.count)": 3 << (Swift.min(1, jsonO.count))]
      }
       var writen: Bool = true
          var filer: String! = String(cString: [111,95,57,49,95,109,111,110,105,116,111,114,0], encoding: .utf8)!
          var verifyi: String! = String(cString: [110,111,110,99,101,0], encoding: .utf8)!
          var historyn: String! = String(cString: [99,95,54,50,95,112,108,97,99,101,109,101,110,116,0], encoding: .utf8)!
         r_widthw.append("\((3 - (writen ? 2 : 1)))")
         filer.append("\((filer == (String(cString:[119,0], encoding: .utf8)!) ? filer.count : verifyi.count))")
         verifyi.append("\(1)")
         historyn = "\(filer.count)"
      repeat {
          var recordss: String! = String(cString: [115,95,49,48,95,112,114,101,118,101,110,116,101,100,0], encoding: .utf8)!
          var sectionsZ: [Any]! = [985, 290, 221]
          var firste: [String: Any]! = [String(cString: [119,114,105,116,101,108,111,99,107,0], encoding: .utf8)!:String(cString: [114,101,110,101,119,95,57,95,57,49,0], encoding: .utf8)!, String(cString: [105,115,108,111,119,0], encoding: .utf8)!:String(cString: [115,95,49,55,95,105,115,112,111,115,97,98,108,101,0], encoding: .utf8)!, String(cString: [98,97,116,99,104,0], encoding: .utf8)!:String(cString: [108,111,97,100,101,100,0], encoding: .utf8)!]
         register_g4.append("\(register_g4.count / (Swift.max(1, sectionsZ.count)))")
         recordss.append("\(recordss.count)")
         sectionsZ.append((recordss == (String(cString:[55,0], encoding: .utf8)!) ? recordss.count : firste.values.count))
         firste[recordss] = recordss.count << (Swift.min(labs(2), 1))
         if (String(cString:[99,106,108,103,108,53,0], encoding: .utf8)!) == register_g4 {
            break
         }
      } while ((String(cString:[99,106,108,103,108,53,0], encoding: .utf8)!) == register_g4) && (2 <= register_g4.count)
         writen = !register_g4.hasSuffix("\(writen)")
         parameterd.append("\(parameterd.count)")
      while (!parameterd.hasPrefix("\(register_g4.count)")) {
         parameterd.append("\(parameterd.count)")
         break
      }
         register_g4.append("\(1)")
      recordingvP = "\(r_widthw.count)"
     var labeelListen: String! = String(cString: [105,110,115,116,97,108,108,101,100,0], encoding: .utf8)!
     let commonBase: Double = 8505.0
    var  validationsParcor = [Any]()
    validationsParcor.append(labeelListen)
    validationsParcor.append(commonBase)

    return validationsParcor

}





    
    func onNuiTtsEventCallback(_ event: NuiSdkTtsEvent, taskId taskid: UnsafeMutablePointer<CChar>!, code: Int32) {

             navigationVerticallyChannelUnique(pasteboardPaint:8997.0, valuelabelAnswer:true, timerHandle:[String(cString: [112,97,112,101,114,115,95,99,95,56,53,0], encoding: .utf8)!:[String(cString: [114,95,49,48,48,95,109,117,110,109,97,112,0], encoding: .utf8)!:274, String(cString: [99,108,99,112,0], encoding: .utf8)!:336]])


       var collR: String! = String(cString: [102,105,114,115,116,0], encoding: .utf8)!
    var containso: [String: Any]! = [String(cString: [102,114,101,101,116,121,112,101,0], encoding: .utf8)!:80, String(cString: [115,117,99,99,101,101,100,0], encoding: .utf8)!:692, String(cString: [99,97,114,114,105,97,103,101,0], encoding: .utf8)!:480]
    var sheetd: String! = String(cString: [108,111,99,97,108,105,122,97,116,105,111,110,0], encoding: .utf8)!
      sheetd = "\(3 & containso.count)"

       var emptyt: Double = 5.0
       var dismiss2: String! = String(cString: [101,120,112,114,101,115,115,105,111,110,115,0], encoding: .utf8)!
      if 4.23 == emptyt {
         emptyt += (Double((String(cString:[118,0], encoding: .utf8)!) == dismiss2 ? dismiss2.count : Int(emptyt)))
      }
      while (emptyt <= Double(dismiss2.count)) {
          var processk: Double = 1.0
         dismiss2.append("\(3)")
         processk += Double(Int(processk))
         break
      }
         dismiss2 = "\(3 - dismiss2.count)"
         emptyt -= (Double(dismiss2 == (String(cString:[81,0], encoding: .utf8)!) ? Int(emptyt) : dismiss2.count))
          var detailR: Bool = false
          var actionZ: String! = String(cString: [97,116,116,114,105,98,115,0], encoding: .utf8)!
         dismiss2 = "\(Int(emptyt))"
         detailR = actionZ.hasSuffix("\(detailR)")
         actionZ.append("\(3)")
         emptyt /= Swift.max(Double(Int(emptyt)), 2)
      containso = ["\(containso.values.count)": 1 ^ containso.values.count]
            
       if event == NuiSdkTtsEvent(rawValue: 0) {
           
           do {
               try AVAudioSession.sharedInstance().setCategory(.playback, options: .mixWithOthers)
       var tabbarC: String! = String(cString: [108,119,115,115,112,110,0], encoding: .utf8)!
       var editJ: String! = String(cString: [98,105,110,107,98,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
         editJ = "\(3)"
      }
       var delegate_675: [String: Any]! = [String(cString: [98,105,103,117,105,110,116,0], encoding: .utf8)!:String(cString: [114,103,98,121,117,118,0], encoding: .utf8)!, String(cString: [97,99,107,110,111,119,108,101,100,103,101,100,0], encoding: .utf8)!:String(cString: [103,101,116,104,100,114,0], encoding: .utf8)!]
         editJ = "\(3)"
      while (1 > (delegate_675.keys.count + 3) || (editJ.count + 3) > 3) {
         delegate_675 = ["\(delegate_675.values.count)": 1]
         break
      }
         delegate_675["\(editJ)"] = editJ.count * 1
          var areaQ: [Any]! = [589, 977]
          var gressy: String! = String(cString: [110,95,50,48,0], encoding: .utf8)!
          var layoutu: Int = 1
         editJ.append("\(gressy.count / 2)")
         areaQ.append(layoutu)
         gressy = "\(layoutu)"
      containso = [editJ: editJ.count]
      tabbarC.append("\(tabbarC.count)")

   repeat {
       var rmblabel4: String! = String(cString: [105,100,101,110,116,105,116,105,101,115,0], encoding: .utf8)!
      repeat {
         rmblabel4 = "\(rmblabel4.count + 1)"
         if (String(cString:[53,51,101,101,99,118,48,105,57,0], encoding: .utf8)!) == rmblabel4 {
            break
         }
      } while ((String(cString:[53,51,101,101,99,118,48,105,57,0], encoding: .utf8)!) == rmblabel4) && (rmblabel4.count == rmblabel4.count)
         rmblabel4 = "\(((String(cString:[110,0], encoding: .utf8)!) == rmblabel4 ? rmblabel4.count : rmblabel4.count))"
      repeat {
         rmblabel4 = "\(rmblabel4.count)"
         if rmblabel4.count == 3441716 {
            break
         }
      } while (rmblabel4.hasSuffix(rmblabel4)) && (rmblabel4.count == 3441716)
      sheetd.append("\(3 * collR.count)")
      if (String(cString:[97,120,108,0], encoding: .utf8)!) == sheetd {
         break
      }
   } while ((String(cString:[97,120,108,0], encoding: .utf8)!) == sheetd) && (sheetd.count == collR.count)
               try AVAudioSession.sharedInstance().setActive(true)
           } catch {
               print("Failed to set audio session category: \(error)")
           }
           voicePlayer.play()
   for _ in 0 ..< 1 {
      collR = "\(sheetd.count)"
   }
           self.completionHandler?(.ZJiaoPopup)

       } else if event == NuiSdkTtsEvent(rawValue: 1) || event == NuiSdkTtsEvent(rawValue: 2) || event == NuiSdkTtsEvent(rawValue: 5) {
           if event == NuiSdkTtsEvent(rawValue: 1) {
               
               voicePlayer.drain()
           } else {
               
           }
           if event == NuiSdkTtsEvent(rawValue: 5) {
               stopPlay(false)
      containso = ["\(containso.values.count)": 2 & containso.values.count]
               let level = utils.nui_tts_get_param("error_msg")
               print(level)
           }
       }
    }

@discardableResult
 func executeParameterBigAreaIntermediateFinite() -> String! {
    var evetx: Bool = true
    var phone5: Int = 5
   while (phone5 < 3) {
      evetx = !evetx
      break
   }
      evetx = phone5 == 42
      evetx = phone5 == 75 && evetx
      phone5 /= Swift.max(((evetx ? 3 : 1) / (Swift.max(phone5, 7))), 1)
     var ylabelDown: String! = String(cString: [100,114,111,112,115,95,119,95,52,53,0], encoding: .utf8)!
     var pictureValuelabel: Float = 5305.0
     let launchNumber: Int = 4706
     let preferredGenerator: Double = 5671.0
    var movetoDrainPeers = String()
    movetoDrainPeers += ylabelDown

    return movetoDrainPeers

}





    
    func onNuiTtsVolumeCallback(_ volume: Int32, taskId task_id: UnsafeMutablePointer<CChar>!) {

             executeParameterBigAreaIntermediateFinite()


       var findg: Double = 3.0
    var speechg: Double = 4.0
      speechg /= Swift.max(3, Double(Int(speechg) >> (Swift.min(labs(Int(findg)), 4))))
   while (5.18 < findg) {
       var remarkx: String! = String(cString: [102,101,97,116,117,114,101,100,0], encoding: .utf8)!
      if remarkx.hasPrefix("\(remarkx.count)") {
         remarkx = "\(1)"
      }
      while (remarkx != remarkx) {
          var recordingvy: Double = 0.0
          var textg: String! = String(cString: [114,116,112,112,108,97,121,0], encoding: .utf8)!
          var hasV: String! = String(cString: [97,100,103,114,111,117,112,0], encoding: .utf8)!
          var versionlabelc: String! = String(cString: [98,111,110,100,0], encoding: .utf8)!
          var recognition8: String! = String(cString: [120,109,108,0], encoding: .utf8)!
         remarkx.append("\(3)")
         recordingvy *= Double(Int(recordingvy) % (Swift.max(1, 4)))
         textg.append("\(versionlabelc.count >> (Swift.min(labs(2), 2)))")
         hasV.append("\(3)")
         versionlabelc = "\(versionlabelc.count - hasV.count)"
         recognition8.append("\(recognition8.count >> (Swift.min(labs(1), 3)))")
         break
      }
         remarkx = "\(remarkx.count * remarkx.count)"
      speechg /= Swift.max(4, Double(remarkx.count | 2))
      break
   }

      speechg += Double(Int(findg) * Int(speechg))
      speechg -= Double(Int(findg))
    
        debugPrint("——————音频波纹：\(volume)")
    
    }

    
    func playerDidFinish() {
       var launch8: Bool = false
    var normala: String! = String(cString: [104,97,108,102,108,116,117,105,110,116,0], encoding: .utf8)!
      launch8 = normala.count >= 52
   if normala.hasSuffix("\(launch8)") {
      launch8 = launch8 || normala.count < 92
   }

   while (normala.count > 1 && launch8) {
      normala.append("\(((launch8 ? 3 : 2)))")
      break
   }
       var saveG: String! = String(cString: [100,105,115,99,111,110,110,101,99,116,101,100,0], encoding: .utf8)!
       var zhidingese: String! = String(cString: [116,105,109,101,111,117,116,0], encoding: .utf8)!
       var picker3: String! = String(cString: [97,100,118,97,110,99,101,0], encoding: .utf8)!
          var modityB: String! = String(cString: [97,110,110,111,116,97,116,101,0], encoding: .utf8)!
          var safev: Int = 4
          var x_objectQ: Float = 3.0
         picker3.append("\(Int(x_objectQ))")
         modityB.append("\(3)")
         safev >>= Swift.min(3, labs(safev))
         x_objectQ /= Swift.max(1, Float(safev))
          var register_wM: [Any]! = [String(cString: [103,101,116,108,98,108,111,99,107,105,110,99,0], encoding: .utf8)!, String(cString: [105,115,114,101,97,100,111,110,108,121,0], encoding: .utf8)!, String(cString: [112,114,117,110,101,0], encoding: .utf8)!]
          var ossl: String! = String(cString: [99,121,99,108,101,99,108,111,99,107,0], encoding: .utf8)!
          var descript3: Int = 1
         saveG.append("\(ossl.count ^ 2)")
         register_wM = [2]
         ossl.append("\(descript3)")
         descript3 |= register_wM.count & 3
      launch8 = saveG.count == 8 && launch8
        
         stopPlay()
    
    }
}


class GNManagerItems: NSObject {
private var has_offset: Double = 0.0
private var phoneDescriptString: String!
var linePadding: Float = 0.0
var answerHeightInt_2hDict: [String: Any]!



    
    var generateStrExpireList: [Any]!
    var has_Request: Bool = false
    
    private let documentPicker = SFSpeechRecognizer(locale: Locale(identifier: "zh-CN"))!
    private var answerBottom: SFSpeechAudioBufferRecognitionRequest?
    private var lengthGif: SFSpeechRecognitionTask?
    private let audioEngine = AVAudioEngine()
    private var voiceHandler: Bool = false
    private var isDetectionEnabled: Bool
    var speakTimer: DispatchSourceTimer?
    private let popupRawing: TimeInterval = 3.0
    private let chooseNum = UIImpactFeedbackGenerator(style: .light)

    var resultHandler: ((String) -> Void)?
    var decibelScaleHandler: ((Float) -> Void)?

    init(isDetectionEnabled: Bool = false) {
        self.isDetectionEnabled = isDetectionEnabled
        self.chooseNum.prepare()
    }

@discardableResult
 func dispatchLikeRemain() -> String! {
    var sheetf: Int = 5
    var beforeb: Float = 4.0
   if (Float(sheetf) / (Swift.max(beforeb, 2))) < 4.72 && (sheetf / (Swift.max(Int(beforeb), 3))) < 3 {
       var presentd: Int = 5
      while (1 >= (3 ^ presentd)) {
         presentd %= Swift.max(2, 1 << (Swift.min(2, labs(presentd))))
         break
      }
      if (3 % (Swift.max(1, presentd))) >= 3 && 1 >= (presentd % 3) {
          var launchv: [String: Any]! = [String(cString: [114,95,56,54,95,115,117,98,109,101,115,115,97,103,101,0], encoding: .utf8)!:741, String(cString: [112,114,111,112,111,115,97,108,0], encoding: .utf8)!:305]
          var clearr: String! = String(cString: [114,103,98,116,111,98,103,114,95,52,95,49,48,0], encoding: .utf8)!
          var rmbA: String! = String(cString: [114,119,103,116,0], encoding: .utf8)!
          var phoneh: String! = String(cString: [100,121,110,98,117,102,0], encoding: .utf8)!
         presentd -= rmbA.count
         launchv = [phoneh: 1]
         clearr = "\((clearr == (String(cString:[52,0], encoding: .utf8)!) ? phoneh.count : clearr.count))"
         rmbA.append("\(clearr.count)")
      }
         presentd *= 2
      sheetf <<= Swift.min(labs(sheetf | 1), 1)
   }
       var alifastK: Bool = false
      repeat {
         alifastK = (alifastK ? alifastK : !alifastK)
         if alifastK ? !alifastK : alifastK {
            break
         }
      } while (alifastK ? !alifastK : alifastK) && (alifastK)
          var speedsD: String! = String(cString: [109,97,110,97,103,101,115,0], encoding: .utf8)!
          var indicesW: Int = 4
         alifastK = !speedsD.hasPrefix("\(alifastK)")
         speedsD.append("\(indicesW)")
         alifastK = alifastK && !alifastK
      beforeb += Float(Int(beforeb))
   while (1 < (Int(beforeb) / (Swift.max(1, sheetf))) && (sheetf / (Swift.max(Int(beforeb), 2))) < 1) {
       var e_widthf: String! = String(cString: [114,97,105,115,101,100,0], encoding: .utf8)!
       var observations8: [String: Any]! = [String(cString: [102,97,114,0], encoding: .utf8)!:529, String(cString: [115,119,115,99,97,108,101,95,114,95,55,48,0], encoding: .utf8)!:973, String(cString: [119,97,116,99,104,101,114,115,0], encoding: .utf8)!:332]
       var main_e2: String! = String(cString: [114,101,117,115,97,98,108,101,0], encoding: .utf8)!
      if 2 == (observations8.keys.count & e_widthf.count) && 2 == (2 & observations8.keys.count) {
         observations8 = ["\(observations8.count)": observations8.values.count]
      }
          var suma: String! = String(cString: [115,117,98,118,105,100,101,111,0], encoding: .utf8)!
          var delegate_isb: Double = 3.0
         observations8 = [main_e2: 2 | Int(delegate_isb)]
         suma.append("\(1 << (Swift.min(3, suma.count)))")
         delegate_isb *= Double(1 | suma.count)
          var nameB: String! = String(cString: [118,115,116,97,99,107,97,108,108,111,99,97,116,111,114,0], encoding: .utf8)!
         observations8 = [main_e2: e_widthf.count]
         nameB = "\(nameB.count << (Swift.min(nameB.count, 4)))"
         observations8[main_e2] = observations8.values.count % 3
      for _ in 0 ..< 1 {
         observations8 = ["\(observations8.count)": observations8.count + e_widthf.count]
      }
       var historyO: [Any]! = [697, 783]
      for _ in 0 ..< 1 {
         e_widthf.append("\(3 ^ main_e2.count)")
      }
      repeat {
          var channelC: Bool = false
          var chuang_: [String: Any]! = [String(cString: [99,111,110,116,114,97,105,110,101,100,95,121,95,54,55,0], encoding: .utf8)!:976, String(cString: [114,101,99,101,105,112,116,115,0], encoding: .utf8)!:372]
         observations8 = ["\(historyO.count)": ((String(cString:[90,0], encoding: .utf8)!) == main_e2 ? historyO.count : main_e2.count)]
         channelC = !channelC
         chuang_ = ["\(chuang_.keys.count)": ((channelC ? 4 : 3) - 1)]
         if observations8.count == 2145559 {
            break
         }
      } while (observations8.count == 2145559) && (4 <= (4 >> (Swift.min(1, e_widthf.count))))
          var speechD: String! = String(cString: [114,101,112,108,97,121,101,114,95,113,95,54,49,0], encoding: .utf8)!
          var w_managerh: String! = String(cString: [100,117,97,108,105,110,112,117,116,0], encoding: .utf8)!
          var q_playerf: String! = String(cString: [105,115,97,108,110,117,109,0], encoding: .utf8)!
         main_e2.append("\(3)")
         speechD.append("\(3)")
         w_managerh.append("\(3 % (Swift.max(6, q_playerf.count)))")
         q_playerf = "\(speechD.count)"
      beforeb /= Swift.max(Float(Int(beforeb) * 1), 5)
      break
   }
   if (sheetf * Int(beforeb)) <= 2 && (2 << (Swift.min(3, labs(sheetf)))) <= 3 {
      sheetf &= Int(beforeb)
   }
     var thinkingConverted: Double = 4177.0
     let zhidingesEnabled: Bool = true
    var attractingPanning = String()

    return attractingPanning

}






    func cancelRecording() {

             dispatchLikeRemain()


       var versionV: String! = String(cString: [100,97,105,108,121,0], encoding: .utf8)!
    var launchX: String! = String(cString: [112,108,97,121,101,114,0], encoding: .utf8)!
   if launchX.contains(versionV) {
      launchX.append("\(launchX.count ^ versionV.count)")
   }

   if versionV.count > launchX.count {
       var jiaoE: String! = String(cString: [116,101,110,99,0], encoding: .utf8)!
       var navQ: Int = 1
       var pressY: [String: Any]! = [String(cString: [108,101,116,116,101,114,115,0], encoding: .utf8)!:275, String(cString: [100,105,114,101,99,116,105,111,110,115,0], encoding: .utf8)!:851, String(cString: [114,101,99,101,112,116,105,111,110,0], encoding: .utf8)!:287]
      while (jiaoE.count <= 1) {
          var networkT: String! = String(cString: [97,100,100,114,105,110,102,111,0], encoding: .utf8)!
          var playing5: Float = 1.0
         navQ /= Swift.max(navQ << (Swift.min(labs(3), 4)), 3)
         networkT.append("\(3 - networkT.count)")
         playing5 /= Swift.max(2, Float(networkT.count % 1))
         break
      }
          var pasteboard3: Double = 0.0
          var iconJ: String! = String(cString: [105,112,97,100,105,102,102,0], encoding: .utf8)!
          var likeV: String! = String(cString: [98,114,117,115,104,95,120,95,54,54,0], encoding: .utf8)!
         navQ %= Swift.max(1, navQ - pressY.values.count)
         pasteboard3 /= Swift.max(4, Double(iconJ.count << (Swift.min(5, labs(Int(pasteboard3))))))
         iconJ = "\(Int(pasteboard3))"
         likeV = "\(iconJ.count)"
         navQ >>= Swift.min(labs(3 ^ navQ), 2)
         navQ += ((String(cString:[51,0], encoding: .utf8)!) == jiaoE ? jiaoE.count : pressY.keys.count)
      repeat {
          var messageN: String! = String(cString: [117,115,100,0], encoding: .utf8)!
          var gressg: String! = String(cString: [97,114,114,97,121,0], encoding: .utf8)!
          var levelu: Double = 2.0
         jiaoE.append("\(Int(levelu))")
         messageN = "\(2 ^ gressg.count)"
         gressg = "\(messageN.count)"
         levelu *= Double(2)
         if jiaoE == (String(cString:[117,115,98,114,0], encoding: .utf8)!) {
            break
         }
      } while (3 < (jiaoE.count << (Swift.min(labs(3), 3)))) && (jiaoE == (String(cString:[117,115,98,114,0], encoding: .utf8)!))
      for _ in 0 ..< 1 {
          var receivet: [String: Any]! = [String(cString: [111,112,116,105,111,110,115,0], encoding: .utf8)!:25.0]
         navQ |= receivet.keys.count
      }
      repeat {
         navQ ^= jiaoE.count - navQ
         if 419510 == navQ {
            break
         }
      } while (419510 == navQ) && ((navQ & 4) == 2 || 4 == (pressY.count & navQ))
      while ((2 + jiaoE.count) >= 4 && 1 >= (jiaoE.count + 2)) {
         pressY = ["\(pressY.count)": navQ | 1]
         break
      }
      while (4 > (2 << (Swift.min(1, labs(navQ))))) {
         navQ *= ((String(cString:[85,0], encoding: .utf8)!) == jiaoE ? jiaoE.count : navQ)
         break
      }
      launchX = "\(navQ & 2)"
   }
        
        voiceHandler = true
   repeat {
      versionV.append("\(launchX.count - versionV.count)")
      if 4870582 == versionV.count {
         break
      }
   } while (4870582 == versionV.count) && (launchX != String(cString:[110,0], encoding: .utf8)!)
        stopRecording()
    }

@discardableResult
 func verifyDetectionLaunchLogChangeFinish(bufferCandidate: Int, navAudio: Bool, eventManager: String!) -> Float {
    var gifd: String! = String(cString: [103,97,109,117,116,0], encoding: .utf8)!
    var b_imagem: Float = 1.0
   if (gifd.count / (Swift.max(3, 6))) == 2 && (3 - gifd.count) == 3 {
      gifd.append("\(((String(cString:[48,0], encoding: .utf8)!) == gifd ? Int(b_imagem) : gifd.count))")
   }
   while ((5 >> (Swift.min(4, gifd.count))) < 3) {
       var substring1: [Any]! = [String(cString: [119,95,52,54,95,114,101,108,97,116,105,118,101,108,121,0], encoding: .utf8)!, String(cString: [115,117,105,116,101,115,0], encoding: .utf8)!]
       var layoutV: String! = String(cString: [114,117,110,0], encoding: .utf8)!
       var aidaD: Double = 3.0
       var detection1: [String: Any]! = [String(cString: [109,103,109,116,0], encoding: .utf8)!:459, String(cString: [118,95,56,54,95,115,105,109,117,108,97,116,101,100,0], encoding: .utf8)!:849]
         detection1 = ["\(substring1.count)": substring1.count]
      while ((aidaD * 5.26) > 5.50 && 2 > (layoutV.count & 5)) {
         layoutV = "\(substring1.count)"
         break
      }
      repeat {
          var nextN: String! = String(cString: [109,97,107,101,0], encoding: .utf8)!
          var n_objectm: Double = 0.0
         aidaD += Double(3)
         nextN.append("\(2)")
         n_objectm -= Double(nextN.count ^ 1)
         if 1930834.0 == aidaD {
            break
         }
      } while (4 <= layoutV.count) && (1930834.0 == aidaD)
         layoutV.append("\(3)")
         layoutV = "\(layoutV.count / 3)"
      repeat {
         aidaD -= Double(substring1.count)
         if aidaD == 3630498.0 {
            break
         }
      } while (aidaD == 3630498.0) && (5 >= (Int(aidaD) / (Swift.max(detection1.values.count, 1))) && 5 >= (detection1.values.count / (Swift.max(1, Int(aidaD)))))
         layoutV.append("\(1 ^ substring1.count)")
      for _ in 0 ..< 1 {
          var pickeri: String! = String(cString: [97,116,116,114,97,99,116,105,111,110,0], encoding: .utf8)!
          var graphicsm: [String: Any]! = [String(cString: [115,108,111,112,95,103,95,54,50,0], encoding: .utf8)!:5552.0]
          var regionD: [String: Any]! = [String(cString: [112,114,101,115,115,105,110,103,95,97,95,54,0], encoding: .utf8)!:String(cString: [119,111,114,107,115,0], encoding: .utf8)!, String(cString: [115,116,97,116,101,112,0], encoding: .utf8)!:String(cString: [115,117,98,0], encoding: .utf8)!, String(cString: [112,95,57,48,95,98,111,111,108,101,97,110,0], encoding: .utf8)!:String(cString: [109,105,110,105,109,97,108,0], encoding: .utf8)!]
         layoutV.append("\(pickeri.count)")
         pickeri.append("\(graphicsm.count)")
         graphicsm = ["\(regionD.values.count)": graphicsm.values.count]
         regionD = ["\(regionD.count)": regionD.keys.count * 2]
      }
      if 1 == (1 / (Swift.max(9, Int(aidaD)))) && (aidaD / (Swift.max(2.22, 10))) == 3.2 {
         layoutV.append("\(substring1.count - 1)")
      }
      if layoutV.count > 2 {
          var sectiono: Float = 2.0
          var while_6t: String! = String(cString: [112,110,103,100,115,112,95,117,95,55,52,0], encoding: .utf8)!
          var queryr: String! = String(cString: [112,95,57,53,95,97,99,99,101,115,115,105,98,105,108,105,116,121,0], encoding: .utf8)!
         substring1.append(Int(aidaD))
         sectiono += Float(3)
         while_6t.append("\((queryr == (String(cString:[101,0], encoding: .utf8)!) ? queryr.count : while_6t.count))")
      }
       var instanceP: String! = String(cString: [116,105,99,107,101,114,95,115,95,52,50,0], encoding: .utf8)!
         layoutV.append("\(detection1.values.count << (Swift.min(labs(3), 2)))")
      b_imagem -= Float(3)
      break
   }
   repeat {
       var replaceV: [Any]! = [[571, 123, 226]]
       var yloadingh: Float = 2.0
       var scrollf: [String: Any]! = [String(cString: [117,95,51,48,95,114,101,99,101,105,112,116,115,0], encoding: .utf8)!:913, String(cString: [118,100,101,98,117,103,95,114,95,50,55,0], encoding: .utf8)!:128, String(cString: [102,105,114,101,119,97,108,108,0], encoding: .utf8)!:712]
       var headersd: Int = 4
       var remarkK: Double = 2.0
      while (3.25 < (yloadingh / 5.15) && 5.15 < (yloadingh / (Swift.max(Float(replaceV.count), 10)))) {
         replaceV = [scrollf.count & 1]
         break
      }
      repeat {
          var descriptF: Double = 5.0
          var numberlabelx: String! = String(cString: [110,95,51,48,95,111,119,110,101,100,0], encoding: .utf8)!
          var relationg: Int = 4
         remarkK += Double(Int(yloadingh))
         descriptF -= Double(relationg | numberlabelx.count)
         numberlabelx.append("\(3 << (Swift.min(1, labs(relationg))))")
         if 4040939.0 == remarkK {
            break
         }
      } while (4040939.0 == remarkK) && (headersd < Int(remarkK))
          var fastS: [String: Any]! = [String(cString: [99,109,97,112,95,52,95,57,52,0], encoding: .utf8)!:808, String(cString: [111,103,103,100,101,99,95,56,95,50,48,0], encoding: .utf8)!:276]
          var avatarsh: String! = String(cString: [117,112,112,101,114,99,97,115,101,0], encoding: .utf8)!
          var decibely: Bool = false
         remarkK /= Swift.max(2, (Double((String(cString:[73,0], encoding: .utf8)!) == avatarsh ? avatarsh.count : Int(yloadingh))))
         fastS = ["\(fastS.count)": ((decibely ? 4 : 2) % (Swift.max(3, 9)))]
         decibely = fastS["\(decibely)"] == nil
         headersd /= Swift.max(2 | scrollf.count, 5)
      for _ in 0 ..< 1 {
         remarkK -= Double(headersd)
      }
       var controll2: Double = 2.0
      if 1.27 <= controll2 {
         controll2 += Double(3 & Int(remarkK))
      }
      if Double(headersd) == controll2 {
          var browserN: String! = String(cString: [104,117,101,115,95,54,95,56,53,0], encoding: .utf8)!
          var number9: String! = String(cString: [100,101,105,110,116,101,114,108,97,99,101,95,102,95,50,53,0], encoding: .utf8)!
          var historyx: [String: Any]! = [String(cString: [109,111,100,101,120,112,0], encoding: .utf8)!:1000, String(cString: [117,110,100,101,114,114,117,110,0], encoding: .utf8)!:145]
         headersd >>= Swift.min(1, labs(historyx.values.count / 2))
         browserN.append("\(number9.count)")
         number9.append("\(number9.count * 3)")
         historyx[number9] = browserN.count
      }
         scrollf["\(yloadingh)"] = Int(controll2)
      for _ in 0 ..< 1 {
         controll2 *= Double(Int(remarkK) * 1)
      }
       var detailslabelj: Float = 1.0
      repeat {
          var detailF: String! = String(cString: [97,95,57,51,95,114,111,116,97,116,105,111,110,0], encoding: .utf8)!
          var minen: String! = String(cString: [103,98,114,97,112,95,122,95,52,48,0], encoding: .utf8)!
         yloadingh *= (Float(detailF == (String(cString:[86,0], encoding: .utf8)!) ? detailF.count : Int(remarkK)))
         minen.append("\(2)")
         if 4026320.0 == yloadingh {
            break
         }
      } while ((yloadingh * Float(remarkK)) == 4.24 || 4.60 == (4.24 * remarkK)) && (4026320.0 == yloadingh)
      while (4.56 <= (Double(headersd * Int(yloadingh)))) {
         headersd &= Int(remarkK)
         break
      }
         remarkK *= Double(Int(detailslabelj))
         headersd <<= Swift.min(labs(3), 3)
      gifd.append("\(3)")
      if gifd.count == 3246951 {
         break
      }
   } while (Int(b_imagem) >= gifd.count) && (gifd.count == 3246951)
      gifd = "\(((String(cString:[108,0], encoding: .utf8)!) == gifd ? Int(b_imagem) : gifd.count))"
   return b_imagem

}






    
    func startRecording() {

             verifyDetectionLaunchLogChangeFinish(bufferCandidate:3660, navAudio:true, eventManager:String(cString: [122,95,52,49,95,97,114,114,0], encoding: .utf8)!)


        
        chooseNum.impactOccurred()
        audioEngine.stop()
        audioEngine.reset()

        DispatchQueue.main.asyncAfter(deadline: .now() + 0.1) { [weak self] in
            
            guard let self = self else { return }

            checkAliToken()
            
            if let lengthGif = self.lengthGif {
                lengthGif.cancel()
                self.lengthGif = nil
            }

            self.voiceHandler = false
            
            let create = AVAudioSession.sharedInstance()
            try! create.setCategory(.record, mode: .measurement, options: .duckOthers)
            try! create.setActive(true, options: .notifyOthersOnDeactivation)

            self.answerBottom = SFSpeechAudioBufferRecognitionRequest()

            let enabled = self.audioEngine.inputNode

            guard let answerBottom = self.answerBottom else { fatalError("Unable to create a SFSpeechAudioBufferRecognitionRequest object") }

            answerBottom.shouldReportPartialResults = true
            
            self.lengthGif = self.documentPicker.recognitionTask(with: answerBottom) { result, error in
                var formatterButton = false
                
                if let result = result {
                    
                    if self.isDetectionEnabled {
                        self.rootLastVideoClearApply()
                        self.alwaysToolBackgroundFirstLayer()
                    }
                    print("——————说话中: \(result.bestTranscription.formattedString)")
                    formatterButton = result.isFinal
                    NotificationCenter.default.post(name: NSNotification.Name("VoiceMessageNotificationName"), object: result.bestTranscription.formattedString)
                }

                if error != nil || formatterButton {
                    self.audioEngine.stop()
                    enabled.removeTap(onBus: 0)

                    self.answerBottom = nil
                    self.lengthGif = nil

                    if let result = result {
                        if self.voiceHandler == false {
                            self.resultHandler?(result.bestTranscription.formattedString)
                        }
                    }
                }
            }

            let register_l = enabled.outputFormat(forBus: 0)
            enabled.installTap(onBus: 0, bufferSize: 1024, format: register_l) { (buffer: AVAudioPCMBuffer, when: AVAudioTime) in
                self.answerBottom?.append(buffer)
                
                
                guard let channelData = buffer.floatChannelData else { return }
                
                
                let avatarView = vDSP_Length(buffer.frameLength)
                var play: Float = 0
                vDSP_rmsqv(channelData[0], 1, &play, avatarView)
                
                
                let tap = 20 * log10(play)
                
                
                DispatchQueue.main.async {
                    
                    let checkc = 1.0 + CGFloat((tap + 50) / 50.0)
                    let mine = max(1.0, min(checkc, 1.5))
                    
                    if mine > 1.0 {
                        self.decibelScaleHandler?(Float(mine))
                    }
                }
            }
            
            self.audioEngine.prepare()
            try! self.audioEngine.start()
        }
        
    }

@discardableResult
 func minimumEqualPrivacyReadPointFind(restoreLogo: [Any]!, writeNetwork: String!, uploadAvatar: Bool) -> Float {
    var sectionZ: Double = 2.0
    var bottomZ: Double = 5.0
   for _ in 0 ..< 3 {
       var statuslabelN: String! = String(cString: [113,122,98,105,110,0], encoding: .utf8)!
       var n_playera: String! = String(cString: [115,117,112,101,114,102,114,97,109,101,0], encoding: .utf8)!
         n_playera = "\(n_playera.count)"
      while (n_playera.count >= 4) {
          var verifyh: Float = 2.0
          var isbdingR: Float = 3.0
         n_playera.append("\(Int(isbdingR))")
         verifyh += Float(1 - Int(verifyh))
         break
      }
         statuslabelN = "\(n_playera.count)"
      while (!n_playera.contains(statuslabelN)) {
         statuslabelN.append("\(3)")
         break
      }
      while (statuslabelN == n_playera) {
         n_playera.append("\(3 * n_playera.count)")
         break
      }
      for _ in 0 ..< 1 {
          var placeholderL: Double = 2.0
          var icon5: Double = 4.0
         statuslabelN.append("\(((String(cString:[99,0], encoding: .utf8)!) == n_playera ? statuslabelN.count : n_playera.count))")
         placeholderL *= Double(Int(icon5))
      }
      bottomZ -= Double(1 | n_playera.count)
   }
   repeat {
      sectionZ *= Double(2)
      if sectionZ == 3792408.0 {
         break
      }
   } while (1.4 == (Double(4 / (Swift.max(10, Int(bottomZ)))))) && (sectionZ == 3792408.0)
      bottomZ *= Double(1 * Int(bottomZ))
      bottomZ += Double(2 ^ Int(bottomZ))
     var playingGesture: String! = String(cString: [97,117,116,111,102,105,108,108,95,121,95,55,49,0], encoding: .utf8)!
     var stringUrl: String! = String(cString: [111,112,116,105,109,105,122,101,114,0], encoding: .utf8)!
    var branchExtractedEveryone:Float = 0

    return branchExtractedEveryone

}






    private func alwaysToolBackgroundFirstLayer() {

             minimumEqualPrivacyReadPointFind(restoreLogo:[String(cString: [112,108,97,121,97,98,108,101,0], encoding: .utf8)!, String(cString: [108,105,115,116,101,110,101,114,0], encoding: .utf8)!, String(cString: [109,97,105,108,116,111,0], encoding: .utf8)!], writeNetwork:String(cString: [115,104,111,116,0], encoding: .utf8)!, uploadAvatar:false)


       var instanceE: Int = 3
    var videoc: Bool = false
   while (1 > instanceE) {
      instanceE ^= 2
      break
   }

   repeat {
       var tabbarn: String! = String(cString: [112,114,101,100,0], encoding: .utf8)!
       var loginq: Bool = false
       var commonw: Int = 0
      if commonw > tabbarn.count {
         commonw /= Swift.max(1 * tabbarn.count, 3)
      }
      if !tabbarn.contains("\(loginq)") {
          var holderlabelE: String! = String(cString: [105,110,110,101,114,0], encoding: .utf8)!
          var listen1: [String: Any]! = [String(cString: [98,105,103,110,117,109,0], encoding: .utf8)!:String(cString: [100,105,97,109,111,110,100,0], encoding: .utf8)!]
          var while_0fA: Float = 0.0
          var failedv: String! = String(cString: [109,97,110,97,103,101,109,101,110,116,0], encoding: .utf8)!
         loginq = (while_0fA / (Swift.max(1, Float(listen1.count)))) >= 99.100
         holderlabelE = "\(failedv.count >> (Swift.min(labs(3), 5)))"
         listen1 = [failedv: (holderlabelE == (String(cString:[107,0], encoding: .utf8)!) ? holderlabelE.count : failedv.count)]
         while_0fA *= Float(2)
      }
         tabbarn = "\(tabbarn.count)"
      while (loginq) {
         loginq = tabbarn.count >= 15 || loginq
         break
      }
      for _ in 0 ..< 2 {
         commonw %= Swift.max((tabbarn.count & (loginq ? 4 : 2)), 3)
      }
      videoc = loginq && 92 > instanceE
      if videoc ? !videoc : videoc {
         break
      }
   } while (!videoc && (instanceE - 1) <= 2) && (videoc ? !videoc : videoc)

      instanceE /= Swift.max(instanceE, 1)
        if speakTimer == nil {
            speakTimer?.cancel()
   if instanceE > 4 {
      videoc = instanceE <= 86
   }
            speakTimer = DispatchSource.makeTimerSource(queue: DispatchQueue.main)
            speakTimer?.schedule(deadline: .now() + popupRawing, repeating: .never)
            speakTimer?.setEventHandler { [weak self] in
                self?.stopRecording()
            }
            speakTimer?.resume()
        }
    }


    func stopRecording() {
       var presentM: String! = String(cString: [116,111,110,101,109,97,112,0], encoding: .utf8)!
    var o_centerF: Bool = true
    var placeL: String! = String(cString: [103,114,111,117,112,110,97,109,101,115,0], encoding: .utf8)!
   while (placeL.count >= presentM.count) {
      placeL.append("\(((o_centerF ? 5 : 5) - presentM.count))")
      break
   }

      placeL = "\(((String(cString:[100,0], encoding: .utf8)!) == placeL ? placeL.count : (o_centerF ? 4 : 4)))"
        
        chooseNum.impactOccurred()
        
        DispatchQueue.main.asyncAfter(deadline: .now() + 0.1) { [weak self] in
            guard let self = self else { return }

   if !o_centerF {
      o_centerF = placeL.count == presentM.count
   }
            self.rootLastVideoClearApply()
   if o_centerF {
      o_centerF = presentM.count >= 18 && o_centerF
   }
            self.audioEngine.stop()
      presentM.append("\(((o_centerF ? 3 : 5) | placeL.count))")
            self.answerBottom?.endAudio()
       var attributesy: Int = 2
       var rawing0: String! = String(cString: [114,116,112,115,101,110,100,101,114,0], encoding: .utf8)!
          var convertedI: String! = String(cString: [97,116,114,97,99,116,97,98,0], encoding: .utf8)!
          var refreshU: Float = 2.0
         rawing0 = "\(rawing0.count)"
         convertedI = "\(Int(refreshU))"
         refreshU -= Float(Int(refreshU) % (Swift.max(convertedI.count, 6)))
      while (5 > (5 << (Swift.min(4, labs(attributesy)))) && (5 << (Swift.min(5, labs(attributesy)))) > 5) {
         attributesy &= rawing0.count ^ 1
         break
      }
          var detailsn: Double = 4.0
          var while_3t: String! = String(cString: [119,104,105,116,101,115,0], encoding: .utf8)!
         attributesy |= while_3t.count / (Swift.max(2, 8))
         detailsn /= Swift.max(Double(Int(detailsn)), 2)
         while_3t.append("\(Int(detailsn) + 2)")
         attributesy %= Swift.max(1, rawing0.count ^ 1)
         attributesy &= 3
      repeat {
         attributesy |= 2
         if attributesy == 256870 {
            break
         }
      } while (attributesy == 256870) && (rawing0.hasSuffix("\(attributesy)"))
      o_centerF = presentM.count >= 82
            let create = AVAudioSession.sharedInstance()
            try! create.setCategory(.playback, mode: .default)
            try! create.setActive(true, options: .notifyOthersOnDeactivation)
        }
        
    }


    
    private func rootLastVideoClearApply() {
       var sliderH: [String: Any]! = [String(cString: [100,100,114,97,110,103,101,0], encoding: .utf8)!:517, String(cString: [97,110,110,101,120,98,109,112,0], encoding: .utf8)!:671]
    var pasteboard_: Bool = false
   for _ in 0 ..< 3 {
       var self_p8: Bool = false
       var verticall: String! = String(cString: [119,101,105,103,104,116,115,0], encoding: .utf8)!
       var datej: Double = 3.0
      for _ in 0 ..< 1 {
         self_p8 = !self_p8
      }
         verticall = "\((Int(datej) - (self_p8 ? 4 : 5)))"
      if verticall.contains("\(self_p8)") {
          var main_ua: Bool = false
          var cancelC: String! = String(cString: [99,111,110,100,101,110,115,101,100,0], encoding: .utf8)!
          var recordsN: String! = String(cString: [108,105,116,116,108,101,0], encoding: .utf8)!
         self_p8 = cancelC == (String(cString:[87,0], encoding: .utf8)!) && verticall.count > 21
         main_ua = ((recordsN.count + (!main_ua ? 40 : recordsN.count)) == 40)
         cancelC = "\(recordsN.count / 2)"
      }
      if 2 > (verticall.count << (Swift.min(labs(3), 2))) || (3 + verticall.count) > 4 {
         datej /= Swift.max(Double(verticall.count), 3)
      }
         verticall.append("\(((self_p8 ? 5 : 4) * Int(datej)))")
      for _ in 0 ..< 1 {
          var disconnectl: [Any]! = [[763, 424]]
          var actionX: Bool = false
          var baseh: Bool = false
          var screenW: String! = String(cString: [108,105,98,109,0], encoding: .utf8)!
         self_p8 = !self_p8
         disconnectl.append(disconnectl.count)
         actionX = disconnectl.count >= 12
         baseh = ((disconnectl.count >> (Swift.min(4, labs((actionX ? 68 : disconnectl.count))))) < 68)
         screenW = "\(3)"
      }
         verticall = "\(verticall.count * 2)"
          var modelt: [Any]! = [534, 188]
         verticall.append("\(3)")
         modelt = [modelt.count]
         self_p8 = !self_p8 && verticall.count >= 49
      pasteboard_ = sliderH.keys.count >= 22 && pasteboard_
   }
   if !pasteboard_ || (2 << (Swift.min(2, sliderH.keys.count))) < 5 {
       var infox: String! = String(cString: [103,101,116,115,111,99,107,111,112,116,0], encoding: .utf8)!
         infox.append("\(1)")
         infox = "\(2 ^ infox.count)"
      repeat {
         infox = "\(infox.count + infox.count)"
         if infox.count == 1973386 {
            break
         }
      } while (infox.count > infox.count) && (infox.count == 1973386)
      pasteboard_ = (((!pasteboard_ ? infox.count : 66) / (Swift.max(infox.count, 9))) >= 66)
   }

   while (!pasteboard_) {
       var bundle4: String! = String(cString: [107,95,51,51,0], encoding: .utf8)!
       var speakO: [Any]! = [String(cString: [99,114,101,97,116,105,111,110,0], encoding: .utf8)!, String(cString: [114,101,103,105,115,116,114,97,114,0], encoding: .utf8)!]
       var appM: Double = 0.0
          var eveantu: Float = 4.0
         speakO = [speakO.count >> (Swift.min(bundle4.count, 1))]
         eveantu /= Swift.max(5, Float(Int(eveantu) << (Swift.min(4, labs(Int(eveantu))))))
      while ((speakO.count / 3) <= 4 || (speakO.count - Int(appM)) <= 3) {
         speakO = [Int(appM)]
         break
      }
          var filee: [String: Any]! = [String(cString: [110,100,101,102,0], encoding: .utf8)!:121, String(cString: [112,114,101,100,105,99,116,111,114,0], encoding: .utf8)!:368, String(cString: [109,101,109,98,101,114,115,104,105,112,0], encoding: .utf8)!:455]
         bundle4.append("\(filee.count * 2)")
         speakO = [Int(appM)]
       var avatar5: Float = 2.0
      repeat {
         speakO.append(Int(avatar5) & 2)
         if speakO.count == 4393252 {
            break
         }
      } while (4 <= speakO.count) && (speakO.count == 4393252)
       var contentO: String! = String(cString: [107,105,108,111,98,121,116,101,0], encoding: .utf8)!
       var fromd: String! = String(cString: [101,114,97,115,101,114,0], encoding: .utf8)!
      while (!fromd.contains(contentO)) {
         contentO = "\(2)"
         break
      }
          var headersf: Bool = true
         fromd = "\(3)"
      pasteboard_ = sliderH.values.count > speakO.count
      break
   }
      sliderH["\(pasteboard_)"] = ((pasteboard_ ? 4 : 1) ^ sliderH.count)

        speakTimer?.cancel()
        speakTimer = nil
    }
    
}
