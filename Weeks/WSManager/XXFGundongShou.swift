
import Foundation

import UIKit
import YBPopupMenu

class XXFGundongShou: NSObject {
var stylesChange_string: String!
private var strTableAlifastArr: [Any]?
var normalMargin: Double = 0.0



    
    static let show = XXFGundongShou()

@discardableResult
 func aboutFeedbackCancel(playingFind: [Any]!) -> [String: Any]! {
    var removeV: Float = 3.0
    var picture7: String! = String(cString: [115,105,110,103,108,101,116,97,98,108,101,0], encoding: .utf8)!
      removeV -= Float(3)
   if picture7.contains("\(removeV)") {
       var socketF: Int = 5
       var update_rs6: String! = String(cString: [100,101,99,114,121,112,116,101,100,0], encoding: .utf8)!
       var screenk: String! = String(cString: [102,111,110,116,115,0], encoding: .utf8)!
          var notification5: Double = 4.0
          var c_view8: Double = 5.0
          var applicationF: String! = String(cString: [119,104,105,108,101,0], encoding: .utf8)!
         socketF /= Swift.max(2, (update_rs6 == (String(cString:[74,0], encoding: .utf8)!) ? update_rs6.count : socketF))
         notification5 -= Double(Int(c_view8))
         c_view8 *= Double(2 & Int(notification5))
         applicationF = "\(applicationF.count)"
      while (screenk != String(cString:[112,0], encoding: .utf8)!) {
          var actionl: String! = String(cString: [97,108,101,114,116,115,0], encoding: .utf8)!
          var change9: Int = 1
          var g_playerz: [String: Any]! = [String(cString: [104,95,52,54,95,109,100,97,116,101,0], encoding: .utf8)!:String(cString: [100,101,109,117,120,101,114,0], encoding: .utf8)!, String(cString: [106,115,105,109,100,101,120,116,0], encoding: .utf8)!:String(cString: [109,112,106,112,101,103,0], encoding: .utf8)!, String(cString: [114,116,109,112,112,107,116,0], encoding: .utf8)!:String(cString: [99,111,110,115,116,114,117,99,116,111,114,95,100,95,51,51,0], encoding: .utf8)!]
          var currenti: Float = 5.0
         update_rs6 = "\(socketF * Int(currenti))"
         actionl.append("\(change9 % (Swift.max(6, g_playerz.count)))")
         change9 <<= Swift.min(actionl.count, 4)
         g_playerz["\(change9)"] = change9
         currenti += Float(change9)
         break
      }
         update_rs6 = "\((screenk == (String(cString:[112,0], encoding: .utf8)!) ? update_rs6.count : screenk.count))"
          var answerJ: String! = String(cString: [104,112,105,99,95,101,95,52,52,0], encoding: .utf8)!
         update_rs6 = "\((update_rs6 == (String(cString:[113,0], encoding: .utf8)!) ? screenk.count : update_rs6.count))"
         answerJ = "\(answerJ.count)"
      for _ in 0 ..< 2 {
          var speedsh: Double = 2.0
          var reusable1: Bool = false
         screenk = "\(socketF)"
         speedsh += (Double((reusable1 ? 3 : 4) >> (Swift.min(labs(Int(speedsh)), 2))))
         reusable1 = !reusable1
      }
       var ringr: String! = String(cString: [99,117,116,101,115,116,0], encoding: .utf8)!
       var sectionsH: Double = 4.0
       var contentsR: Double = 2.0
      for _ in 0 ..< 3 {
         socketF >>= Swift.min(labs(socketF), 2)
      }
          var handlerx: Double = 4.0
         contentsR += Double(Int(sectionsH))
         handlerx /= Swift.max(Double(Int(handlerx)), 4)
      picture7.append("\(picture7.count * 3)")
   }
   for _ in 0 ..< 2 {
      removeV *= Float(3 % (Swift.max(7, picture7.count)))
   }
      picture7.append("\(picture7.count & 3)")
     var convertedAction: Bool = true
     var zhidingesSection: Bool = true
     var convertedInstance: Float = 8754.0
    var redactedCroll = [String: Any]()
    convertedAction = false
    redactedCroll.updateValue(convertedAction, forKey:String(cString: [54,0], encoding: .utf8)!)
    zhidingesSection = false
    redactedCroll.updateValue(zhidingesSection, forKey:String(cString: [80,0], encoding: .utf8)!)
    convertedInstance += 46
    redactedCroll.updateValue(convertedInstance, forKey:String(cString: [119,0], encoding: .utf8)!)

    return redactedCroll

}





    
    func action_showPopupMenu(for view: UIView, width: CGFloat, items: [TListItem]) {

             aboutFeedbackCancel(playingFind:[654, 113, 746])


       var socketD: Float = 2.0
    var voiceP: Float = 1.0
    var convertedR: Bool = true
      voiceP -= Float(Int(socketD))

        YBPopupMenu.showRely(on: view, titles: items, icons: nil, menuWidth: width) { popupMenu in
       var configS: String! = String(cString: [108,111,116,116,105,101,0], encoding: .utf8)!
         configS = "\(configS.count)"
         configS.append("\(3)")
      while (configS == String(cString:[86,0], encoding: .utf8)!) {
         configS = "\(configS.count)"
         break
      }
      convertedR = !convertedR
            
            popupMenu?.tableView.separatorStyle = .none
       var playingc: Bool = false
       var alabela: Double = 1.0
         alabela /= Swift.max((Double((playingc ? 5 : 2) % (Swift.max(1, Int(alabela))))), 5)
         alabela *= Double(Int(alabela) / 2)
          var graphics5: String! = String(cString: [106,100,109,97,105,110,99,116,0], encoding: .utf8)!
          var ringq: String! = String(cString: [119,111,114,100,115,0], encoding: .utf8)!
          var viptimelabel8: Float = 1.0
         alabela -= (Double((String(cString:[115,0], encoding: .utf8)!) == ringq ? Int(viptimelabel8) : ringq.count))
         graphics5.append("\(graphics5.count + 3)")
          var scalex: Double = 1.0
          var sockete: Int = 3
          var login5: String! = String(cString: [112,114,111,116,101,99,116,111,114,0], encoding: .utf8)!
         playingc = !playingc
         scalex -= Double(sockete + login5.count)
         sockete >>= Swift.min(login5.count, 5)
          var cancelA: String! = String(cString: [112,100,115,0], encoding: .utf8)!
          var fileN: [Any]! = [false]
         alabela /= Swift.max((Double((playingc ? 2 : 1))), 5)
         cancelA.append("\(((String(cString:[49,0], encoding: .utf8)!) == cancelA ? cancelA.count : fileN.count))")
         fileN = [fileN.count]
          var cancelz: String! = String(cString: [101,110,99,105,114,99,108,101,100,0], encoding: .utf8)!
         playingc = cancelz.count >= 63 || !playingc
      socketD *= Float(2 * Int(voiceP))
            popupMenu?.tableView.separatorColor = UIColor(red: 169 / 255.0, green: 253 / 255.0, blue: 54 / 255.0, alpha: 1)
   if socketD >= 5.80 {
      voiceP -= (Float((convertedR ? 4 : 4) << (Swift.min(labs(Int(socketD)), 3))))
   }
            popupMenu?.minSpace = 12
      socketD /= Swift.max(3, Float(Int(socketD) | 3))
            popupMenu?.itemHeight = 44
      convertedR = 25.89 == voiceP
            popupMenu?.showMaskView = false
            popupMenu?.isShowShadow = false
            popupMenu?.arrowWidth = 12
            popupMenu?.arrowHeight = 4
            popupMenu?.animationManager.duration = 0.15
            popupMenu?.delegate = self
            popupMenu?.backColor = .clear
            popupMenu?.tableView.register(UINib(nibName: "PSpeedsCell", bundle: nil), forCellReuseIdentifier: "cell")
            popupMenu?.tableView.layer.cornerRadius = 10
        }
    }
    
}

extension XXFGundongShou: YBPopupMenuDelegate {

@discardableResult
 func tapCacheRestoreCreateDeadline(msgRate: Double, isbdingGenerator: String!, qlabelSpeeds: Double) -> Int {
    var r_image4: Double = 0.0
    var int_zzx: [Any]! = [1295.0]
   if 5 > (int_zzx.count % 1) {
      r_image4 /= Swift.max(2, Double(Int(r_image4) * 2))
   }
   if (Double(int_zzx.count) + r_image4) > 2.97 && (Double(int_zzx.count) + r_image4) > 2.97 {
      r_image4 *= Double(1 + int_zzx.count)
   }
      int_zzx.append(int_zzx.count / (Swift.max(3, 5)))
     let avatarsRequest: Bool = true
     var validateBottom: String! = String(cString: [112,114,111,104,105,98,105,116,0], encoding: .utf8)!
    var unmatchedPanicTimebase:Int = 0
    unmatchedPanicTimebase /= Int(avatarsRequest ? 71 : 27)

    return unmatchedPanicTimebase

}





    func ybPopupMenu(_ ybPopupMenu: YBPopupMenu!, cellForRowAt index: Int) -> UITableViewCell! {

             tapCacheRestoreCreateDeadline(msgRate:2039.0, isbdingGenerator:String(cString: [114,101,111,112,101,110,0], encoding: .utf8)!, qlabelSpeeds:3620.0)


       var sublyoutv: Float = 2.0
    var executeW: String! = String(cString: [100,101,110,111,114,109,97,108,0], encoding: .utf8)!
    var itemV: String! = String(cString: [100,99,113,117,97,110,116,0], encoding: .utf8)!
      executeW = "\(executeW.count + Int(sublyoutv))"

      sublyoutv *= (Float((String(cString:[72,0], encoding: .utf8)!) == executeW ? executeW.count : Int(sublyoutv)))
        let labelV = IndexPath(row: index, section: 0)
   repeat {
       var accountlabelL: String! = String(cString: [112,97,114,116,105,99,105,112,97,110,116,115,0], encoding: .utf8)!
       var remarku: String! = String(cString: [114,101,103,117,108,97,116,101,0], encoding: .utf8)!
       var hasC: String! = String(cString: [116,101,114,109,115,0], encoding: .utf8)!
       var gundong9: String! = String(cString: [110,97,109,101,115,101,114,118,101,114,115,0], encoding: .utf8)!
       var dico: Double = 3.0
      if 4 < hasC.count {
         hasC = "\(remarku.count | gundong9.count)"
      }
      for _ in 0 ..< 2 {
         accountlabelL.append("\(hasC.count >> (Swift.min(labs(3), 5)))")
      }
      repeat {
         accountlabelL = "\(remarku.count)"
         if 2012331 == accountlabelL.count {
            break
         }
      } while (2012331 == accountlabelL.count) && (remarku.count >= accountlabelL.count)
          var messageU: String! = String(cString: [118,108,105,110,101,0], encoding: .utf8)!
         dico /= Swift.max(5, Double(Int(dico)))
         messageU = "\(3)"
      if hasC.hasPrefix(gundong9) {
          var avatars0: [Any]! = [655, 198, 741]
          var summ: String! = String(cString: [102,102,109,97,116,104,0], encoding: .utf8)!
          var decibelL: [Any]! = [String(cString: [101,110,104,97,110,99,101,114,0], encoding: .utf8)!, String(cString: [100,99,97,100,101,99,0], encoding: .utf8)!]
         hasC = "\(decibelL.count * gundong9.count)"
         avatars0.append(avatars0.count - 1)
         summ = "\(2)"
         decibelL.append(3 * summ.count)
      }
      repeat {
          var collectionA: String! = String(cString: [115,101,112,105,97,0], encoding: .utf8)!
          var strN: Double = 2.0
         hasC = "\((remarku == (String(cString:[53,0], encoding: .utf8)!) ? Int(strN) : remarku.count))"
         collectionA.append("\(3)")
         strN /= Swift.max(4, Double(2))
         if (String(cString:[101,56,49,118,102,102,0], encoding: .utf8)!) == hasC {
            break
         }
      } while (remarku == hasC) && ((String(cString:[101,56,49,118,102,102,0], encoding: .utf8)!) == hasC)
         gundong9 = "\(remarku.count)"
      while (accountlabelL.count > gundong9.count) {
         accountlabelL.append("\(1)")
         break
      }
      while ((Int(dico) - accountlabelL.count) >= 4 && (accountlabelL.count / (Swift.max(4, 1))) >= 5) {
          var candidate7: String! = String(cString: [99,116,105,109,101,0], encoding: .utf8)!
          var alamofireh: String! = String(cString: [114,101,115,101,114,118,101,115,105,122,101,0], encoding: .utf8)!
         accountlabelL.append("\(1)")
         candidate7.append("\(alamofireh.count)")
         alamofireh.append("\(candidate7.count & 1)")
         break
      }
      for _ in 0 ..< 3 {
          var timerJ: Double = 2.0
          var selected8: String! = String(cString: [105,110,116,101,114,99,101,112,116,0], encoding: .utf8)!
          var actionl: String! = String(cString: [120,100,97,105,0], encoding: .utf8)!
          var resultR: String! = String(cString: [98,117,105,108,100,115,0], encoding: .utf8)!
         dico /= Swift.max(Double(gundong9.count), 3)
         timerJ -= Double(Int(timerJ) - 2)
         selected8.append("\(2 >> (Swift.min(3, selected8.count)))")
         actionl = "\(1)"
         resultR.append("\(actionl.count & selected8.count)")
      }
       var baseb: Double = 3.0
      for _ in 0 ..< 3 {
         hasC.append("\(remarku.count)")
      }
      for _ in 0 ..< 3 {
         accountlabelL = "\(hasC.count)"
      }
         baseb -= Double(gundong9.count | accountlabelL.count)
      if 5.37 >= (baseb / (Swift.max(Double(accountlabelL.count), 9))) {
         accountlabelL.append("\(accountlabelL.count ^ hasC.count)")
      }
      itemV.append("\(Int(sublyoutv))")
      if (String(cString:[102,114,97,110,102,97,57,100,48,113,0], encoding: .utf8)!) == itemV {
         break
      }
   } while ((String(cString:[102,114,97,110,102,97,57,100,48,113,0], encoding: .utf8)!) == itemV) && (!itemV.hasPrefix("\(sublyoutv)"))
        let main_mCell = ybPopupMenu.tableView.dequeueReusableCell(withIdentifier: "cell", for: labelV) as! PSpeedsCell
      sublyoutv += (Float((String(cString:[99,0], encoding: .utf8)!) == executeW ? executeW.count : itemV.count))
        let aidItem = ybPopupMenu.titles[index] as! TListItem
       var rmblabelz: Double = 4.0
       var accountlabelk: Double = 3.0
      for _ in 0 ..< 2 {
         accountlabelk += Double(1 & Int(accountlabelk))
      }
      while ((rmblabelz - 3.99) <= 1.83) {
         rmblabelz *= Double(Int(rmblabelz) % (Swift.max(6, Int(accountlabelk))))
         break
      }
       var recordingvd: Bool = true
       var pickedl: Bool = false
         rmblabelz *= (Double(1 | (pickedl ? 3 : 1)))
         recordingvd = (accountlabelk * rmblabelz) > 37.42
         rmblabelz -= (Double(Int(rmblabelz) * (recordingvd ? 1 : 5)))
      executeW.append("\(Int(sublyoutv))")
        main_mCell.label.text = aidItem.title
      itemV.append("\(itemV.count & executeW.count)")
        main_mCell.icon.image = aidItem.image
        return main_mCell
    }

    
    func ybPopupMenuDidDismiss(_ ybPopupMenu: YBPopupMenu!) {
       var toolse: Bool = false
    var sourceA: [String: Any]! = [String(cString: [98,105,110,100,105,110,103,115,0], encoding: .utf8)!:UILabel()]
    var likeu: [String: Any]! = [String(cString: [97,118,101,114,97,103,101,105,105,114,100,0], encoding: .utf8)!:295, String(cString: [112,97,117,115,101,0], encoding: .utf8)!:491]
   if sourceA["\(likeu.values.count)"] != nil {
      sourceA["\(toolse)"] = ((toolse ? 2 : 5) / (Swift.max(1, 4)))
   }
   repeat {
       var bigW: String! = String(cString: [116,109,112,0], encoding: .utf8)!
       var gifX: Double = 3.0
       var markE: Float = 4.0
       var collectionH: [Any]! = [String(cString: [109,117,108,116,105,101,110,100,0], encoding: .utf8)!, String(cString: [119,105,114,101,102,114,97,109,101,0], encoding: .utf8)!, String(cString: [122,101,114,111,99,111,100,101,99,0], encoding: .utf8)!]
          var responseO: String! = String(cString: [105,110,118,105,116,97,116,105,111,110,115,0], encoding: .utf8)!
          var levelh: [Any]! = [String(cString: [98,117,102,102,101,114,115,105,110,107,0], encoding: .utf8)!]
          var areaN: [Any]! = [993, 608, 38]
         collectionH = [responseO.count | bigW.count]
         responseO.append("\(3)")
         levelh = [areaN.count >> (Swift.min(labs(1), 3))]
         areaN = [levelh.count / 1]
      for _ in 0 ..< 3 {
          var elevtc: Float = 2.0
         gifX -= Double(Int(elevtc))
      }
         bigW = "\(Int(gifX))"
      while (gifX < 2.91) {
         markE -= Float(2)
         break
      }
         gifX /= Swift.max(Double(1 >> (Swift.min(labs(Int(markE)), 5))), 2)
         collectionH.append(bigW.count * 2)
      repeat {
         bigW.append("\(bigW.count)")
         if bigW == (String(cString:[114,54,55,0], encoding: .utf8)!) {
            break
         }
      } while (bigW == (String(cString:[114,54,55,0], encoding: .utf8)!)) && (1 > (bigW.count - 4) || (4 - bigW.count) > 5)
      repeat {
         gifX *= Double(bigW.count << (Swift.min(labs(2), 3)))
         if 4411771.0 == gifX {
            break
         }
      } while (4411771.0 == gifX) && (bigW.hasPrefix("\(gifX)"))
      for _ in 0 ..< 1 {
          var window_r8X: Double = 1.0
          var recognitionl: Int = 2
          var pressU: Double = 4.0
          var checkE: String! = String(cString: [119,97,116,99,104,100,111,103,0], encoding: .utf8)!
         markE += Float(Int(window_r8X) % (Swift.max(1, Int(gifX))))
         window_r8X -= Double(checkE.count + 2)
         recognitionl *= 1
         pressU /= Swift.max(Double(2 ^ recognitionl), 1)
         checkE.append("\(checkE.count)")
      }
      if bigW.hasPrefix("\(collectionH.count)") {
         bigW.append("\(Int(markE) ^ collectionH.count)")
      }
         markE /= Swift.max(Float(bigW.count | 3), 5)
      while (bigW.count == 4) {
          var socketM: String! = String(cString: [99,111,109,112,97,114,101,102,0], encoding: .utf8)!
         bigW = "\(socketM.count)"
         break
      }
      toolse = markE <= 16.49 && bigW.count <= 86
      if toolse ? !toolse : toolse {
         break
      }
   } while (!toolse || (3 - sourceA.values.count) == 4) && (toolse ? !toolse : toolse)
   while (4 > (sourceA.keys.count - 3) || toolse) {
      sourceA["\(sourceA.count)"] = likeu.count - sourceA.count
      break
   }


      likeu["\(likeu.keys.count)"] = sourceA.values.count
      likeu["\(toolse)"] = sourceA.count
    }

    
    func ybPopupMenu(_ ybPopupMenu: YBPopupMenu!, didSelectedAt index: Int) {
       var a_layerL: String! = String(cString: [118,109,97,116,114,105,120,0], encoding: .utf8)!
    var converteda: Double = 1.0
       var indicesf: String! = String(cString: [97,114,101,113,117,101,115,116,0], encoding: .utf8)!
       var type_raD: Double = 1.0
       var bar_: String! = String(cString: [114,101,103,105,115,116,114,97,110,116,0], encoding: .utf8)!
         type_raD += (Double((String(cString:[69,0], encoding: .utf8)!) == bar_ ? bar_.count : Int(type_raD)))
      if bar_.hasSuffix("\(type_raD)") {
         type_raD /= Swift.max(Double(bar_.count % 1), 3)
      }
          var header1: String! = String(cString: [99,104,97,105,110,105,100,0], encoding: .utf8)!
          var requests: [Any]! = [678, 62, 0]
          var listL: [Any]! = [String(cString: [97,95,56,55,0], encoding: .utf8)!, String(cString: [116,114,97,110,115,102,111,114,109,101,100,0], encoding: .utf8)!]
         type_raD -= Double(1 * listL.count)
         header1 = "\(2)"
         requests.append(2)
         listL.append(((String(cString:[77,0], encoding: .utf8)!) == header1 ? requests.count : header1.count))
       var begin8: Double = 3.0
       var listenS: Double = 4.0
         begin8 += Double(Int(type_raD))
      if 1.53 > (type_raD - 1.35) || 3.94 > (type_raD - 1.35) {
          var parametersj: Double = 2.0
          var searchb: Float = 2.0
          var downloadM: [String: Any]! = [String(cString: [114,116,112,100,101,99,0], encoding: .utf8)!:177, String(cString: [118,101,114,121,0], encoding: .utf8)!:282, String(cString: [99,111,110,116,97,105,110,0], encoding: .utf8)!:685]
          var scale_: String! = String(cString: [115,97,99,107,0], encoding: .utf8)!
         begin8 += (Double((String(cString:[111,0], encoding: .utf8)!) == bar_ ? bar_.count : Int(type_raD)))
         parametersj /= Swift.max(Double(2), 2)
         searchb /= Swift.max(2, Float(1 / (Swift.max(4, scale_.count))))
         downloadM = ["\(downloadM.keys.count)": downloadM.keys.count << (Swift.min(2, labs(Int(searchb))))]
         scale_.append("\(2 >> (Swift.min(3, scale_.count)))")
      }
         bar_.append("\(Int(begin8) & 2)")
      repeat {
         indicesf.append("\(Int(type_raD))")
         if 749916 == indicesf.count {
            break
         }
      } while (749916 == indicesf.count) && (bar_.count > indicesf.count)
      for _ in 0 ..< 1 {
         bar_ = "\(indicesf.count)"
      }
      a_layerL = "\(Int(type_raD) / (Swift.max(indicesf.count, 4)))"

   for _ in 0 ..< 2 {
      converteda -= Double(3)
   }

       var commonC: String! = String(cString: [101,110,116,105,116,105,101,115,0], encoding: .utf8)!
         commonC.append("\(2)")
      repeat {
         commonC.append("\(commonC.count)")
         if (String(cString:[104,111,107,48,0], encoding: .utf8)!) == commonC {
            break
         }
      } while ((String(cString:[104,111,107,48,0], encoding: .utf8)!) == commonC) && (commonC.hasPrefix("\(commonC.count)"))
      for _ in 0 ..< 1 {
          var listu: String! = String(cString: [100,101,99,101,108,101,114,97,116,101,100,0], encoding: .utf8)!
          var aide: String! = String(cString: [99,97,108,108,98,97,99,107,0], encoding: .utf8)!
          var detailsI: String! = String(cString: [109,97,100,100,0], encoding: .utf8)!
          var not_zx: [Any]! = [String(cString: [114,101,115,101,114,118,101,0], encoding: .utf8)!, String(cString: [116,105,99,107,101,114,0], encoding: .utf8)!, String(cString: [105,112,100,111,112,100,0], encoding: .utf8)!]
         commonC.append("\(not_zx.count)")
         listu.append("\(listu.count)")
         aide.append("\(1 - detailsI.count)")
         detailsI = "\(2)"
         not_zx.append(listu.count)
      }
      a_layerL.append("\(2 ^ commonC.count)")
        let aidItem = ybPopupMenu.titles[index] as! TListItem
   repeat {
      a_layerL.append("\((a_layerL == (String(cString:[90,0], encoding: .utf8)!) ? Int(converteda) : a_layerL.count))")
      if 2829751 == a_layerL.count {
         break
      }
   } while (2829751 == a_layerL.count) && (a_layerL.hasPrefix("\(converteda)"))
        if aidItem.executeWhileDismissComplete {
            
        }
        
        if let action = aidItem.action {
            action()
        }
        
    }
}
